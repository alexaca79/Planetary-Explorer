<commit_history>
  <current_branch>
    feature/chat-history-downloads
  </current_branch>

  <base_branch>
    719044a74b5ca13b2a527aac3d10ef5520cc95d9
  </base_branch>

  <commits>
  <commit hash="21cf7e6" date="2026-08-26"><message><subject><![CDATA[feat(chat): add durable history and test bundles]]></subject><body><![CDATA[- persist owner-scoped sessions in Cosmos DB
  
  - store attachments in Blob and export replayable ZIP bundles
  
  - add responsive history UI and focused tests
  
  ≡ƒùé∩╕Å - Generated by Copilot
  ]]></body></message></commit>
  </commits>

  <full_diff>
  diff --git a/planetary-explorer/container-app/chat_history_api.py b/planetary-explorer/container-app/chat_history_api.py
  new file mode 100644
  index 0000000..327da94
  --- /dev/null
  +++ b/planetary-explorer/container-app/chat_history_api.py
  @@ -0,0 +1,333 @@
  +"""Authenticated chat-history, attachment, and test-bundle API routes."""
  +
  +from __future__ import annotations
  +
  +import json
  +import logging
  +import os
  +from tempfile import SpooledTemporaryFile
  +from typing import Any, Awaitable, TypeVar
  +from urllib.parse import quote
  +from zipfile import ZIP_DEFLATED, ZipFile
  +
  +from fastapi import APIRouter, Depends, File, HTTPException, Request, Response, UploadFile
  +from fastapi.responses import StreamingResponse
  +from starlette.concurrency import run_in_threadpool
  +
  +from chat_history_store import (
  +    MAX_ATTACHMENT_BYTES,
  +    ArtifactStore,
  +    ChatHistoryConflictError,
  +    ChatHistoryError,
  +    ChatHistoryNotFoundError,
  +    ChatHistoryRepository,
  +    ChatHistoryValidationError,
  +    get_artifact_store,
  +    get_chat_history_repository,
  +    public_session_document,
  +    session_summary,
  +)
  +
  +logger = logging.getLogger(__name__)
  +router = APIRouter(prefix="/api/chat-history", tags=["chat-history"])
  +_Result = TypeVar("_Result")
  +MAX_EXPORT_BYTES = 100 * 1024 * 1024
  +
  +
  +def get_request_owner(request: Request) -> str:
  +    """Resolve the stable owner only from middleware-validated claims."""
  +    user = getattr(request.state, "user", {}) or {}
  +    subject = (
  +        user.get("oid")
  +        or user.get("sub")
  +        or user.get("http://schemas.microsoft.com/identity/claims/objectidentifier")
  +    )
  +    tenant = user.get("tid")
  +    if subject:
  +        return f"{tenant}:{subject}" if tenant else str(subject)
  +    if os.environ.get("CHAT_HISTORY_ALLOW_ANONYMOUS", "false").casefold() in {
  +        "1",
  +        "true",
  +        "yes",
  +        "on",
  +    }:
  +        return "local-development"
  +    raise HTTPException(status_code=401, detail="Sign in to use chat history.")
  +
  +
  +async def _history_call(operation: Awaitable[_Result]) -> _Result:
  +    try:
  +        return await operation
  +    except ChatHistoryNotFoundError as exc:
  +        raise HTTPException(status_code=404, detail=str(exc)) from exc
  +    except ChatHistoryConflictError as exc:
  +        raise HTTPException(status_code=409, detail=str(exc)) from exc
  +    except ChatHistoryValidationError as exc:
  +        raise HTTPException(status_code=400, detail=str(exc)) from exc
  +    except ChatHistoryError as exc:
  +        raise HTTPException(status_code=503, detail=str(exc)) from exc
  +    except Exception as exc:
  +        logger.exception("[ChatHistory] persistence operation failed")
  +        raise HTTPException(
  +            status_code=503,
  +            detail="Chat history storage is temporarily unavailable.",
  +        ) from exc
  +
  +
  +def _find_attachment(document: dict[str, Any], attachment_id: str) -> dict[str, Any]:
  +    attachment = next(
  +        (
  +            item
  +            for item in document.get("attachments", [])
  +            if item.get("id") == attachment_id
  +        ),
  +        None,
  +    )
  +    if attachment is None:
  +        raise HTTPException(status_code=404, detail="Attachment not found.")
  +    return attachment
  +
  +
  +def _download_headers(filename: str) -> dict[str, str]:
  +    return {
  +        "Cache-Control": "private, no-store",
  +        "Content-Disposition": f"attachment; filename*=UTF-8''{quote(filename)}",
  +        "X-Content-Type-Options": "nosniff",
  +    }
  +
  +
  +@router.get("/sessions")
  +async def list_chat_sessions(
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +) -> dict[str, Any]:
  +    sessions = await _history_call(repository.list_sessions(owner_id))
  +    return {"sessions": sessions}
  +
  +
  +@router.get("/sessions/{session_id}")
  +async def get_chat_session(
  +    session_id: str,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +) -> dict[str, Any]:
  +    document = await _history_call(repository.get_session(owner_id, session_id))
  +    return public_session_document(document)
  +
  +
  +@router.put("/sessions/{session_id}")
  +async def save_chat_session(
  +    session_id: str,
  +    request: Request,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> dict[str, Any]:
  +    try:
  +        payload = await request.json()
  +    except (json.JSONDecodeError, ValueError) as exc:
  +        raise HTTPException(status_code=400, detail="Session body must be valid JSON.") from exc
  +    if payload.get("expectedRevision", 0) > 0:
  +        existing = await _history_call(repository.get_session(owner_id, session_id))
  +        for attachment in existing.get("attachments", []):
  +            await _history_call(artifacts.touch(attachment))
  +    document = await _history_call(
  +        repository.upsert_session(owner_id, session_id, payload)
  +    )
  +    return public_session_document(document)
  +
  +
  +@router.delete("/sessions/{session_id}", status_code=204)
  +async def delete_chat_session(
  +    session_id: str,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> Response:
  +    document = await _history_call(repository.get_session(owner_id, session_id))
  +    document = await _history_call(
  +        repository.begin_delete(owner_id, session_id, document.get("_etag"))
  +    )
  +    cleanup_failures: list[str] = []
  +    for attachment in document.get("attachments", []):
  +        try:
  +            await artifacts.delete(attachment)
  +        except Exception:
  +            cleanup_failures.append(str(attachment.get("id") or "unknown"))
  +            logger.exception(
  +                "[ChatHistory] orphan cleanup failed for attachment %s",
  +                attachment.get("id"),
  +            )
  +    if cleanup_failures:
  +        raise HTTPException(
  +            status_code=503,
  +            detail="Session files could not be deleted; retry the request.",
  +        )
  +    await _history_call(
  +        repository.delete_session(owner_id, session_id, document.get("_etag"))
  +    )
  +    return Response(status_code=204)
  +
  +
  +@router.post("/sessions/{session_id}/files", status_code=201)
  +async def upload_chat_file(
  +    session_id: str,
  +    file: UploadFile = File(...),
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> dict[str, Any]:
  +    existing = await _history_call(repository.get_session(owner_id, session_id))
  +    for retained in existing.get("attachments", []):
  +        await _history_call(artifacts.touch(retained))
  +    content = bytearray()
  +    while chunk := await file.read(1024 * 1024):
  +        content.extend(chunk)
  +        if len(content) > MAX_ATTACHMENT_BYTES:
  +            raise HTTPException(status_code=413, detail="File exceeds the 25 MB upload limit.")
  +
  +    attachment = await _history_call(
  +        artifacts.upload(
  +            owner_id,
  +            session_id,
  +            file.filename or "data.bin",
  +            file.content_type or "application/octet-stream",
  +            bytes(content),
  +        )
  +    )
  +    try:
  +        document = await _history_call(
  +            repository.add_attachment(owner_id, session_id, attachment)
  +        )
  +    except Exception:
  +        await artifacts.delete(attachment)
  +        raise
  +    return session_summary(document)["attachments"][-1]
  +
  +
  +@router.get("/sessions/{session_id}/files/{attachment_id}")
  +async def download_chat_file(
  +    session_id: str,
  +    attachment_id: str,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> Response:
  +    document = await _history_call(repository.get_session(owner_id, session_id))
  +    attachment = _find_attachment(document, attachment_id)
  +    content = await _history_call(artifacts.download(attachment))
  +    return Response(
  +        content=content,
  +        media_type=attachment.get("contentType") or "application/octet-stream",
  +        headers=_download_headers(attachment["name"]),
  +    )
  +
  +
  +@router.delete("/sessions/{session_id}/files/{attachment_id}", status_code=204)
  +async def delete_chat_file(
  +    session_id: str,
  +    attachment_id: str,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> Response:
  +    document = await _history_call(repository.get_session(owner_id, session_id))
  +    attachment = _find_attachment(document, attachment_id)
  +    for retained in document.get("attachments", []):
  +        if retained.get("id") != attachment_id:
  +            await _history_call(artifacts.touch(retained))
  +    await _history_call(artifacts.delete(attachment))
  +    await _history_call(
  +        repository.remove_attachment(owner_id, session_id, attachment_id)
  +    )
  +    return Response(status_code=204)
  +
  +
  +def _test_bundle_readme(document: dict[str, Any]) -> str:
  +    context = document.get("context", {})
  +    lines = [
  +        "Planetary Explorer chat test bundle",
  +        "",
  +        f"Session: {document.get('sessionId', '')}",
  +        f"Saved: {document.get('updatedAt', '')}",
  +        f"Messages: {document.get('messageCount', 0)}",
  +        f"Model: {context.get('selectedModel') or 'deployment default'}",
  +        f"Module: {context.get('selectedModule') or 'standard chat'}",
  +        "",
  +        "Replay:",
  +        "1. Open session.json and review context and messages.",
  +        "2. Load any files/ entries into the system under test.",
  +        "3. Replay user-role messages in timestamp order.",
  +        "4. Compare answers and tool traces with the saved assistant turns.",
  +        "",
  +        "Secrets, screenshots, signed URL queries, and raw storage paths are omitted.",
  +    ]
  +    return "\n".join(lines)
  +
  +
  +def _write_test_bundle(
  +    archive,
  +    exported: dict[str, Any],
  +    files: list[tuple[dict[str, Any], bytes]],
  +) -> None:
  +    with ZipFile(archive, mode="w", compression=ZIP_DEFLATED) as bundle:
  +        bundle.writestr(
  +            "session.json",
  +            json.dumps(exported, indent=2, ensure_ascii=True).encode("utf-8"),
  +        )
  +        bundle.writestr("README.txt", _test_bundle_readme(exported).encode("utf-8"))
  +        used_names: set[str] = set()
  +        for attachment, content in files:
  +            filename = attachment["name"]
  +            archive_name = f"files/{filename}"
  +            if archive_name in used_names:
  +                archive_name = f"files/{attachment['id']}-{filename}"
  +            used_names.add(archive_name)
  +            bundle.writestr(archive_name, content)
  +
  +
  +@router.get("/sessions/{session_id}/export")
  +async def export_chat_session(
  +    session_id: str,
  +    owner_id: str = Depends(get_request_owner),
  +    repository: ChatHistoryRepository = Depends(get_chat_history_repository),
  +    artifacts: ArtifactStore = Depends(get_artifact_store),
  +) -> StreamingResponse:
  +    document = await _history_call(repository.get_session(owner_id, session_id))
  +    exported = public_session_document(document)
  +    attachments = document.get("attachments", [])
  +    declared_size = sum(int(attachment.get("size") or 0) for attachment in attachments)
  +    if declared_size > MAX_EXPORT_BYTES:
  +        raise HTTPException(
  +            status_code=413,
  +            detail="Test bundle exceeds the 100 MB export limit.",
  +        )
  +
  +    files: list[tuple[dict[str, Any], bytes]] = []
  +    actual_size = 0
  +    for attachment in attachments:
  +        content = await _history_call(artifacts.download(attachment))
  +        actual_size += len(content)
  +        if actual_size > MAX_EXPORT_BYTES:
  +            raise HTTPException(
  +                status_code=413,
  +                detail="Test bundle exceeds the 100 MB export limit.",
  +            )
  +        files.append((attachment, content))
  +
  +    archive = SpooledTemporaryFile(max_size=8 * 1024 * 1024, mode="w+b")
  +    await run_in_threadpool(_write_test_bundle, archive, exported, files)
  +    archive.seek(0)
  +
  +    def stream_archive():
  +        try:
  +            while chunk := archive.read(1024 * 1024):
  +                yield chunk
  +        finally:
  +            archive.close()
  +
  +    return StreamingResponse(
  +        stream_archive(),
  +        media_type="application/zip",
  +        headers=_download_headers(f"planetary-explorer-{session_id}.zip"),
  +    )
  \ No newline at end of file
  diff --git a/planetary-explorer/container-app/chat_history_store.py b/planetary-explorer/container-app/chat_history_store.py
  new file mode 100644
  index 0000000..29b326c
  --- /dev/null
  +++ b/planetary-explorer/container-app/chat_history_store.py
  @@ -0,0 +1,930 @@
  +"""Owner-isolated chat history persistence contracts and sanitization."""
  +
  +from __future__ import annotations
  +
  +import copy
  +import hashlib
  +import json
  +import os
  +import re
  +import uuid
  +from datetime import datetime, timezone
  +from typing import Any, Protocol
  +from urllib.parse import parse_qsl, urlsplit, urlunsplit
  +
  +
  +MAX_DOCUMENT_BYTES = 1_750_000
  +MAX_ATTACHMENT_BYTES = 25 * 1024 * 1024
  +MAX_ATTACHMENTS = 20
  +MAX_COSMOS_WRITE_ATTEMPTS = 4
  +MAX_MESSAGES = 200
  +MAX_MESSAGE_CONTENT_CHARS = 100_000
  +MAX_TITLE_CHARS = 96
  +
  +_SESSION_ID_PATTERN = re.compile(r"^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$")
  +_MUTATION_ID_PATTERN = re.compile(r"^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$")
  +_URL_PATTERN = re.compile(r"https?://[^\s<>\]\[()]+", re.IGNORECASE)
  +_SENSITIVE_QUERY_KEYS = {
  +    "accesstoken",
  +    "apikey",
  +    "code",
  +    "key",
  +    "sig",
  +    "signature",
  +    "token",
  +}
  +_BLOCKED_KEYS = {
  +    "accesstoken",
  +    "afterscreenshot",
  +    "apikey",
  +    "assets",
  +    "authorization",
  +    "beforescreenshot",
  +    "clientsecret",
  +    "credential",
  +    "imagerybase64",
  +    "password",
  +    "refreshtoken",
  +    "sastoken",
  +    "screenshot",
  +    "secret",
  +    "stacitems",
  +    "token",
  +}
  +_MESSAGE_KEYS = {
  +    "content",
  +    "dataSource",
  +    "role",
  +    "source",
  +    "stacRouting",
  +    "tilesAvailable",
  +    "timestamp",
  +    "toolTrace",
  +    "toolsUsed",
  +}
  +_CONTEXT_KEYS = {
  +    "map",
  +    "pin",
  +    "reasoningEffort",
  +    "selectedDataset",
  +    "selectedModel",
  +    "selectedModule",
  +    "stacMode",
  +}
  +
  +
  +class ChatHistoryError(Exception):
  +    """Base exception for chat-history operations."""
  +
  +
  +class ChatHistoryNotFoundError(ChatHistoryError):
  +    """Raised when a session or attachment is not owned by the caller."""
  +
  +
  +class ChatHistoryConflictError(ChatHistoryError):
  +    """Raised when a client attempts to overwrite a newer transcript."""
  +
  +
  +class ChatHistoryValidationError(ChatHistoryError):
  +    """Raised when a session cannot be safely persisted."""
  +
  +
  +def utc_now_iso() -> str:
  +    """Return the current UTC timestamp in an interoperable ISO format."""
  +    return datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
  +
  +
  +def validate_session_id(session_id: str) -> str:
  +    """Validate and return a client-visible session identifier."""
  +    if not _SESSION_ID_PATTERN.fullmatch(session_id):
  +        raise ChatHistoryValidationError(
  +            "Session id must be 1-128 URL-safe characters."
  +        )
  +    return session_id
  +
  +
  +def opaque_owner_key(owner_id: str) -> str:
  +    """Return a non-reversible owner prefix suitable for blob paths."""
  +    return hashlib.sha256(owner_id.encode("utf-8")).hexdigest()
  +
  +
  +def _redact_sensitive_url(url: str) -> str:
  +    try:
  +        parsed = urlsplit(url)
  +        query_keys = {
  +            re.sub(r"[^a-z0-9]", "", key.casefold())
  +            for key, _ in parse_qsl(parsed.query)
  +        }
  +        if query_keys & _SENSITIVE_QUERY_KEYS:
  +            return urlunsplit((parsed.scheme, parsed.netloc, parsed.path, "<redacted>", ""))
  +    except ValueError:
  +        return url
  +    return url
  +
  +
  +def _sanitize_string(value: str, *, limit: int = MAX_MESSAGE_CONTENT_CHARS) -> str:
  +    redacted = _URL_PATTERN.sub(lambda match: _redact_sensitive_url(match.group(0)), value)
  +    redacted = re.sub(
  +        r"(?i)\bBearer\s+[A-Za-z0-9._~-]+",
  +        "Bearer <redacted>",
  +        redacted,
  +    )
  +    return redacted[:limit]
  +
  +
  +def sanitize_value(value: Any, *, parent_key: str = "") -> Any:
  +    """Strip credentials, screenshots, and oversized catalog payloads recursively."""
  +    if isinstance(value, dict):
  +        sanitized: dict[str, Any] = {}
  +        for raw_key, child in value.items():
  +            key = str(raw_key)
  +            normalized_key = re.sub(r"[^a-z0-9]", "", key.casefold())
  +            if normalized_key in _BLOCKED_KEYS:
  +                continue
  +            sanitized[key] = sanitize_value(child, parent_key=normalized_key)
  +        return sanitized
  +    if isinstance(value, list):
  +        return [sanitize_value(item, parent_key=parent_key) for item in value[:500]]
  +    if isinstance(value, str):
  +        return _sanitize_string(value)
  +    if isinstance(value, (bool, int, float)) or value is None:
  +        return value
  +    return _sanitize_string(str(value))
  +
  +
  +def _normalize_message(raw_message: Any) -> dict[str, Any] | None:
  +    if not isinstance(raw_message, dict):
  +        return None
  +    role = raw_message.get("role")
  +    content = raw_message.get("content")
  +    if role not in {"user", "assistant"} or not isinstance(content, str):
  +        return None
  +
  +    message = {
  +        key: raw_message[key]
  +        for key in _MESSAGE_KEYS
  +        if key in raw_message
  +    }
  +    message["role"] = role
  +    message["content"] = _sanitize_string(content)
  +    message["timestamp"] = _sanitize_string(
  +        str(raw_message.get("timestamp") or utc_now_iso()),
  +        limit=64,
  +    )
  +    return sanitize_value(message)
  +
  +
  +def _derive_title(messages: list[dict[str, Any]]) -> str:
  +    first_user_message = next(
  +        (message["content"] for message in messages if message["role"] == "user"),
  +        "New analysis",
  +    )
  +    compact = re.sub(r"\s+", " ", first_user_message).strip()
  +    return compact[:MAX_TITLE_CHARS] or "New analysis"
  +
  +
  +def normalize_session_document(
  +    owner_id: str,
  +    session_id: str,
  +    payload: dict[str, Any],
  +    *,
  +    existing: dict[str, Any] | None = None,
  +) -> dict[str, Any]:
  +    """Build a bounded Cosmos document from a client session snapshot."""
  +    validate_session_id(session_id)
  +    if not isinstance(payload, dict):
  +        raise ChatHistoryValidationError("Session payload must be a JSON object.")
  +
  +    mutation_id = payload.get("mutationId")
  +    if not isinstance(mutation_id, str) or not _MUTATION_ID_PATTERN.fullmatch(mutation_id):
  +        raise ChatHistoryValidationError(
  +            "mutationId must be 1-128 URL-safe characters."
  +        )
  +    expected_revision = payload.get("expectedRevision")
  +    if not isinstance(expected_revision, int) or expected_revision < 0:
  +        raise ChatHistoryValidationError("expectedRevision must be a non-negative integer.")
  +    existing_revision = int((existing or {}).get("revision") or 0)
  +    if expected_revision != existing_revision:
  +        raise ChatHistoryConflictError(
  +            "Chat session changed since it was loaded; reload before saving."
  +        )
  +
  +    messages = [
  +        normalized
  +        for raw_message in list(payload.get("messages") or [])[-MAX_MESSAGES:]
  +        if (normalized := _normalize_message(raw_message)) is not None
  +    ]
  +    raw_context = payload.get("context") if isinstance(payload.get("context"), dict) else {}
  +    context = sanitize_value({
  +        key: raw_context[key]
  +        for key in _CONTEXT_KEYS
  +        if key in raw_context
  +    })
  +    now = utc_now_iso()
  +    requested_title = payload.get("title")
  +    title = (
  +        re.sub(r"\s+", " ", _sanitize_string(requested_title, limit=MAX_TITLE_CHARS)).strip()
  +        if isinstance(requested_title, str)
  +        else _derive_title(messages)
  +    )
  +    document = {
  +        "id": session_id,
  +        "sessionId": session_id,
  +        "ownerId": owner_id,
  +        "schemaVersion": 1,
  +        "revision": existing_revision + 1,
  +        "lastMutationId": mutation_id,
  +        "title": title or _derive_title(messages),
  +        "createdAt": (existing or {}).get("createdAt") or now,
  +        "updatedAt": now,
  +        "messageCount": len(messages),
  +        "messages": messages,
  +        "context": context,
  +        "attachments": copy.deepcopy((existing or {}).get("attachments") or []),
  +    }
  +    encoded = json.dumps(document, ensure_ascii=True, separators=(",", ":")).encode("utf-8")
  +    if len(encoded) > MAX_DOCUMENT_BYTES:
  +        raise ChatHistoryValidationError(
  +            "Session is too large to save. Start a new chat or remove large tool output."
  +        )
  +    return document
  +
  +
  +def public_session_document(document: dict[str, Any]) -> dict[str, Any]:
  +    """Remove internal owner, blob, and Cosmos metadata before returning a session."""
  +    public = copy.deepcopy(document)
  +    public.pop("ownerId", None)
  +    public.pop("lastMutationId", None)
  +    for key in list(public):
  +        if key.startswith("_"):
  +            public.pop(key, None)
  +    for attachment in public.get("attachments", []):
  +        attachment.pop("blobName", None)
  +    return public
  +
  +
  +def session_summary(document: dict[str, Any]) -> dict[str, Any]:
  +    """Return the compact list representation of a session."""
  +    public = public_session_document(document)
  +    return {
  +        key: public[key]
  +        for key in (
  +            "sessionId",
  +            "title",
  +            "createdAt",
  +            "updatedAt",
  +            "messageCount",
  +            "attachments",
  +        )
  +        if key in public
  +    }
  +
  +
  +class ChatHistoryRepository(Protocol):
  +    """Persistence contract implemented by memory and Cosmos repositories."""
  +
  +    async def list_sessions(self, owner_id: str) -> list[dict[str, Any]]: ...
  +
  +    async def get_session(self, owner_id: str, session_id: str) -> dict[str, Any]: ...
  +
  +    async def upsert_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        payload: dict[str, Any],
  +    ) -> dict[str, Any]: ...
  +
  +    async def begin_delete(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None,
  +    ) -> dict[str, Any]: ...
  +
  +    async def delete_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None = None,
  +    ) -> dict[str, Any]: ...
  +
  +    async def add_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment: dict[str, Any],
  +    ) -> dict[str, Any]: ...
  +
  +    async def remove_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment_id: str,
  +    ) -> dict[str, Any]: ...
  +
  +
  +class ArtifactStore(Protocol):
  +    """Private file persistence used by chat-history attachments."""
  +
  +    async def upload(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        filename: str,
  +        content_type: str,
  +        content: bytes,
  +    ) -> dict[str, Any]: ...
  +
  +    async def download(self, attachment: dict[str, Any]) -> bytes: ...
  +
  +    async def touch(self, attachment: dict[str, Any]) -> None: ...
  +
  +    async def delete(self, attachment: dict[str, Any]) -> None: ...
  +
  +
  +class InMemoryChatHistoryRepository:
  +    """Process-local repository used by local development and unit tests."""
  +
  +    def __init__(self) -> None:
  +        self._documents: dict[tuple[str, str], dict[str, Any]] = {}
  +
  +    async def list_sessions(self, owner_id: str) -> list[dict[str, Any]]:
  +        owned = [
  +            session_summary(document)
  +            for (document_owner, _), document in self._documents.items()
  +            if document_owner == owner_id
  +        ]
  +        return sorted(owned, key=lambda item: item["updatedAt"], reverse=True)
  +
  +    async def get_session(self, owner_id: str, session_id: str) -> dict[str, Any]:
  +        validate_session_id(session_id)
  +        document = self._documents.get((owner_id, session_id))
  +        if document is None:
  +            raise ChatHistoryNotFoundError("Chat session not found.")
  +        return copy.deepcopy(document)
  +
  +    async def upsert_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        payload: dict[str, Any],
  +    ) -> dict[str, Any]:
  +        existing = self._documents.get((owner_id, session_id))
  +        if existing and existing.get("_deleting"):
  +            raise ChatHistoryConflictError("Chat session is being deleted.")
  +        if existing and existing.get("lastMutationId") == payload.get("mutationId"):
  +            return copy.deepcopy(existing)
  +        document = normalize_session_document(
  +            owner_id,
  +            session_id,
  +            payload,
  +            existing=existing,
  +        )
  +        document["_etag"] = f"revision-{document['revision']}"
  +        self._documents[(owner_id, session_id)] = document
  +        return copy.deepcopy(document)
  +
  +    async def begin_delete(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None,
  +    ) -> dict[str, Any]:
  +        document = await self.get_session(owner_id, session_id)
  +        if expected_etag and document.get("_etag") != expected_etag:
  +            raise ChatHistoryConflictError(
  +                "Chat session changed during deletion; retry the request."
  +            )
  +        if not document.get("_deleting"):
  +            document["_deleting"] = True
  +            document["_etag"] = f"deleting-{uuid.uuid4()}"
  +            self._documents[(owner_id, session_id)] = document
  +        return copy.deepcopy(document)
  +
  +    async def delete_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None = None,
  +    ) -> dict[str, Any]:
  +        document = await self.get_session(owner_id, session_id)
  +        if expected_etag and document.get("_etag") != expected_etag:
  +            raise ChatHistoryConflictError(
  +                "Chat session changed during deletion; retry the request."
  +            )
  +        del self._documents[(owner_id, session_id)]
  +        return document
  +
  +    async def add_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment: dict[str, Any],
  +    ) -> dict[str, Any]:
  +        document = await self.get_session(owner_id, session_id)
  +        if document.get("_deleting"):
  +            raise ChatHistoryConflictError("Chat session is being deleted.")
  +        attachments = [
  +            item for item in document.get("attachments", [])
  +            if item.get("id") != attachment.get("id")
  +        ]
  +        if len(attachments) >= MAX_ATTACHMENTS:
  +            raise ChatHistoryValidationError(
  +                f"A session can contain at most {MAX_ATTACHMENTS} files."
  +            )
  +        document["attachments"] = attachments + [copy.deepcopy(attachment)]
  +        document["updatedAt"] = utc_now_iso()
  +        document["_etag"] = f"attachment-{uuid.uuid4()}"
  +        self._documents[(owner_id, session_id)] = document
  +        return copy.deepcopy(document)
  +
  +    async def remove_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment_id: str,
  +    ) -> dict[str, Any]:
  +        document = await self.get_session(owner_id, session_id)
  +        if document.get("_deleting"):
  +            raise ChatHistoryConflictError("Chat session is being deleted.")
  +        attachments = document.get("attachments", [])
  +        removed = next(
  +            (item for item in attachments if item.get("id") == attachment_id),
  +            None,
  +        )
  +        if removed is None:
  +            raise ChatHistoryNotFoundError("Attachment not found.")
  +        document["attachments"] = [
  +            item for item in attachments if item.get("id") != attachment_id
  +        ]
  +        document["updatedAt"] = utc_now_iso()
  +        document["_etag"] = f"attachment-{uuid.uuid4()}"
  +        self._documents[(owner_id, session_id)] = document
  +        return copy.deepcopy(removed)
  +
  +
  +def _is_not_found_error(error: Exception) -> bool:
  +    return getattr(error, "status_code", None) == 404
  +
  +
  +def _is_status_error(error: Exception, status_code: int) -> bool:
  +    return getattr(error, "status_code", None) == status_code
  +
  +
  +class CosmosChatHistoryRepository:
  +    """Azure Cosmos DB for NoSQL repository using Microsoft Entra credentials."""
  +
  +    def __init__(
  +        self,
  +        endpoint: str,
  +        database_name: str,
  +        container_name: str,
  +        *,
  +        container_client: Any | None = None,
  +    ) -> None:
  +        self._endpoint = endpoint
  +        self._database_name = database_name
  +        self._container_name = container_name
  +        self._container = container_client
  +        self._client: Any | None = None
  +        self._credential: Any | None = None
  +
  +    async def _get_container(self) -> Any:
  +        if self._container is not None:
  +            return self._container
  +        try:
  +            from azure.cosmos.aio import CosmosClient
  +            from azure.identity.aio import DefaultAzureCredential
  +        except ImportError as exc:
  +            raise ChatHistoryError(
  +                "Cosmos chat history requires azure-cosmos and azure-identity."
  +            ) from exc
  +
  +        self._credential = DefaultAzureCredential()
  +        self._client = CosmosClient(self._endpoint, credential=self._credential)
  +        database = self._client.get_database_client(self._database_name)
  +        self._container = database.get_container_client(self._container_name)
  +        return self._container
  +
  +    async def list_sessions(self, owner_id: str) -> list[dict[str, Any]]:
  +        container = await self._get_container()
  +        query = (
  +            "SELECT c.sessionId, c.title, c.createdAt, c.updatedAt, "
  +            "c.messageCount, c.attachments FROM c "
  +            "WHERE c.ownerId = @ownerId ORDER BY c.updatedAt DESC"
  +        )
  +        parameters = [{"name": "@ownerId", "value": owner_id}]
  +        results = container.query_items(
  +            query=query,
  +            parameters=parameters,
  +            partition_key=owner_id,
  +        )
  +        return [session_summary(document) async for document in results]
  +
  +    async def get_session(self, owner_id: str, session_id: str) -> dict[str, Any]:
  +        validate_session_id(session_id)
  +        container = await self._get_container()
  +        try:
  +            return await container.read_item(item=session_id, partition_key=owner_id)
  +        except Exception as exc:
  +            if _is_not_found_error(exc):
  +                raise ChatHistoryNotFoundError("Chat session not found.") from exc
  +            raise
  +
  +    async def upsert_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        payload: dict[str, Any],
  +    ) -> dict[str, Any]:
  +        container = await self._get_container()
  +        from azure.core import MatchConditions
  +
  +        for _attempt in range(MAX_COSMOS_WRITE_ATTEMPTS):
  +            try:
  +                existing = await self.get_session(owner_id, session_id)
  +            except ChatHistoryNotFoundError:
  +                document = normalize_session_document(
  +                    owner_id,
  +                    session_id,
  +                    payload,
  +                )
  +                try:
  +                    return await container.create_item(document)
  +                except Exception as exc:
  +                    if _is_status_error(exc, 409):
  +                        continue
  +                    raise
  +
  +            if existing.get("_deleting"):
  +                raise ChatHistoryConflictError("Chat session is being deleted.")
  +            if existing.get("lastMutationId") == payload.get("mutationId"):
  +                return existing
  +
  +            document = normalize_session_document(
  +                owner_id,
  +                session_id,
  +                payload,
  +                existing=existing,
  +            )
  +            try:
  +                return await container.replace_item(
  +                    item=session_id,
  +                    body=document,
  +                    etag=existing.get("_etag"),
  +                    match_condition=MatchConditions.IfNotModified,
  +                )
  +            except Exception as exc:
  +                if _is_status_error(exc, 412):
  +                    continue
  +                raise
  +        raise ChatHistoryError("Chat session changed repeatedly; retry the save.")
  +
  +    async def begin_delete(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None,
  +    ) -> dict[str, Any]:
  +        container = await self._get_container()
  +        from azure.core import MatchConditions
  +
  +        document = await self.get_session(owner_id, session_id)
  +        if expected_etag and document.get("_etag") != expected_etag:
  +            raise ChatHistoryConflictError(
  +                "Chat session changed during deletion; retry the request."
  +            )
  +        if document.get("_deleting"):
  +            return document
  +        document["_deleting"] = True
  +        try:
  +            return await container.replace_item(
  +                item=session_id,
  +                body=document,
  +                etag=document.get("_etag"),
  +                match_condition=MatchConditions.IfNotModified,
  +            )
  +        except Exception as exc:
  +            if _is_status_error(exc, 412):
  +                raise ChatHistoryConflictError(
  +                    "Chat session changed during deletion; retry the request."
  +                ) from exc
  +            raise
  +
  +    async def delete_session(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        expected_etag: str | None = None,
  +    ) -> dict[str, Any]:
  +        document = (
  +            await self.get_session(owner_id, session_id)
  +            if expected_etag is None
  +            else {"_etag": expected_etag}
  +        )
  +        container = await self._get_container()
  +        from azure.core import MatchConditions
  +
  +        try:
  +            await container.delete_item(
  +                item=session_id,
  +                partition_key=owner_id,
  +                etag=expected_etag or document.get("_etag"),
  +                match_condition=MatchConditions.IfNotModified,
  +            )
  +        except Exception as exc:
  +            if _is_status_error(exc, 412):
  +                raise ChatHistoryConflictError(
  +                    "Chat session changed during deletion; retry the request."
  +                ) from exc
  +            raise
  +        return document
  +
  +    async def add_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment: dict[str, Any],
  +    ) -> dict[str, Any]:
  +        container = await self._get_container()
  +        from azure.core import MatchConditions
  +
  +        for _attempt in range(MAX_COSMOS_WRITE_ATTEMPTS):
  +            document = await self.get_session(owner_id, session_id)
  +            if document.get("_deleting"):
  +                raise ChatHistoryConflictError("Chat session is being deleted.")
  +            attachments = [
  +                item for item in document.get("attachments", [])
  +                if item.get("id") != attachment.get("id")
  +            ]
  +            if len(attachments) >= MAX_ATTACHMENTS:
  +                raise ChatHistoryValidationError(
  +                    f"A session can contain at most {MAX_ATTACHMENTS} files."
  +                )
  +            document["attachments"] = attachments + [copy.deepcopy(attachment)]
  +            document["updatedAt"] = utc_now_iso()
  +            try:
  +                return await container.replace_item(
  +                    item=session_id,
  +                    body=document,
  +                    etag=document.get("_etag"),
  +                    match_condition=MatchConditions.IfNotModified,
  +                )
  +            except Exception as exc:
  +                if _is_status_error(exc, 412):
  +                    continue
  +                raise
  +        raise ChatHistoryError("Chat session changed repeatedly; retry the upload.")
  +
  +    async def remove_attachment(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        attachment_id: str,
  +    ) -> dict[str, Any]:
  +        container = await self._get_container()
  +        from azure.core import MatchConditions
  +
  +        for _attempt in range(MAX_COSMOS_WRITE_ATTEMPTS):
  +            document = await self.get_session(owner_id, session_id)
  +            if document.get("_deleting"):
  +                raise ChatHistoryConflictError("Chat session is being deleted.")
  +            attachments = document.get("attachments", [])
  +            removed = next(
  +                (item for item in attachments if item.get("id") == attachment_id),
  +                None,
  +            )
  +            if removed is None:
  +                raise ChatHistoryNotFoundError("Attachment not found.")
  +            document["attachments"] = [
  +                item for item in attachments if item.get("id") != attachment_id
  +            ]
  +            document["updatedAt"] = utc_now_iso()
  +            try:
  +                await container.replace_item(
  +                    item=session_id,
  +                    body=document,
  +                    etag=document.get("_etag"),
  +                    match_condition=MatchConditions.IfNotModified,
  +                )
  +                return copy.deepcopy(removed)
  +            except Exception as exc:
  +                if _is_status_error(exc, 412):
  +                    continue
  +                raise
  +        raise ChatHistoryError("Chat session changed repeatedly; retry the deletion.")
  +
  +
  +def _safe_filename(filename: str) -> str:
  +    normalized = re.sub(r"[\x00-\x1f<>:\"/\\|?*]", "_", filename).strip(" ._")
  +    if not normalized:
  +        raise ChatHistoryValidationError("File name is required.")
  +    return normalized[:160]
  +
  +
  +def _build_attachment(
  +    owner_id: str,
  +    session_id: str,
  +    filename: str,
  +    content_type: str,
  +    content: bytes,
  +) -> dict[str, Any]:
  +    validate_session_id(session_id)
  +    if not content:
  +        raise ChatHistoryValidationError("File is empty.")
  +    if len(content) > MAX_ATTACHMENT_BYTES:
  +        raise ChatHistoryValidationError("File exceeds the 25 MB upload limit.")
  +    attachment_id = str(uuid.uuid4())
  +    safe_filename = _safe_filename(filename)
  +    session_key = hashlib.sha256(session_id.encode("utf-8")).hexdigest()
  +    blob_name = f"{opaque_owner_key(owner_id)}/{session_key}/{attachment_id}/{safe_filename}"
  +    return {
  +        "id": attachment_id,
  +        "name": safe_filename,
  +        "contentType": _sanitize_string(content_type or "application/octet-stream", limit=128),
  +        "size": len(content),
  +        "sha256": hashlib.sha256(content).hexdigest(),
  +        "createdAt": utc_now_iso(),
  +        "blobName": blob_name,
  +    }
  +
  +
  +class InMemoryArtifactStore:
  +    """Process-local artifact storage used in tests and local development."""
  +
  +    def __init__(self) -> None:
  +        self._files: dict[str, bytes] = {}
  +
  +    async def upload(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        filename: str,
  +        content_type: str,
  +        content: bytes,
  +    ) -> dict[str, Any]:
  +        attachment = _build_attachment(
  +            owner_id,
  +            session_id,
  +            filename,
  +            content_type,
  +            content,
  +        )
  +        self._files[attachment["blobName"]] = bytes(content)
  +        return attachment
  +
  +    async def download(self, attachment: dict[str, Any]) -> bytes:
  +        content = self._files.get(str(attachment.get("blobName") or ""))
  +        if content is None:
  +            raise ChatHistoryNotFoundError("Attachment content not found.")
  +        return bytes(content)
  +
  +    async def touch(self, attachment: dict[str, Any]) -> None:
  +        if str(attachment.get("blobName") or "") not in self._files:
  +            raise ChatHistoryNotFoundError("Attachment content not found.")
  +
  +    async def delete(self, attachment: dict[str, Any]) -> None:
  +        self._files.pop(str(attachment.get("blobName") or ""), None)
  +
  +
  +class BlobArtifactStore:
  +    """Private Azure Blob artifact storage using Microsoft Entra credentials."""
  +
  +    def __init__(
  +        self,
  +        endpoint: str,
  +        container_name: str,
  +        *,
  +        container_client: Any | None = None,
  +    ) -> None:
  +        self._endpoint = endpoint
  +        self._container_name = container_name
  +        self._container = container_client
  +        self._service: Any | None = None
  +        self._credential: Any | None = None
  +
  +    async def _get_container(self) -> Any:
  +        if self._container is not None:
  +            return self._container
  +        try:
  +            from azure.identity.aio import DefaultAzureCredential
  +            from azure.storage.blob.aio import BlobServiceClient
  +        except ImportError as exc:
  +            raise ChatHistoryError(
  +                "Chat artifacts require azure-storage-blob and azure-identity."
  +            ) from exc
  +
  +        self._credential = DefaultAzureCredential()
  +        self._service = BlobServiceClient(
  +            account_url=self._endpoint,
  +            credential=self._credential,
  +        )
  +        self._container = self._service.get_container_client(self._container_name)
  +        return self._container
  +
  +    async def upload(
  +        self,
  +        owner_id: str,
  +        session_id: str,
  +        filename: str,
  +        content_type: str,
  +        content: bytes,
  +    ) -> dict[str, Any]:
  +        from azure.storage.blob import ContentSettings
  +
  +        attachment = _build_attachment(
  +            owner_id,
  +            session_id,
  +            filename,
  +            content_type,
  +            content,
  +        )
  +        container = await self._get_container()
  +        blob = container.get_blob_client(attachment["blobName"])
  +        await blob.upload_blob(
  +            content,
  +            overwrite=False,
  +            content_settings=ContentSettings(content_type=attachment["contentType"]),
  +        )
  +        return attachment
  +
  +    async def download(self, attachment: dict[str, Any]) -> bytes:
  +        container = await self._get_container()
  +        blob = container.get_blob_client(attachment["blobName"])
  +        try:
  +            stream = await blob.download_blob()
  +            return await stream.readall()
  +        except Exception as exc:
  +            if _is_not_found_error(exc):
  +                raise ChatHistoryNotFoundError("Attachment content not found.") from exc
  +            raise
  +
  +    async def touch(self, attachment: dict[str, Any]) -> None:
  +        container = await self._get_container()
  +        blob = container.get_blob_client(attachment["blobName"])
  +        try:
  +            await blob.set_blob_metadata(metadata={"session_active_at": utc_now_iso()})
  +        except Exception as exc:
  +            if _is_not_found_error(exc):
  +                raise ChatHistoryNotFoundError("Attachment content not found.") from exc
  +            raise
  +
  +    async def delete(self, attachment: dict[str, Any]) -> None:
  +        container = await self._get_container()
  +        blob = container.get_blob_client(attachment["blobName"])
  +        try:
  +            await blob.delete_blob(delete_snapshots="include")
  +        except Exception as exc:
  +            if not _is_not_found_error(exc):
  +                raise
  +
  +
  +_history_repository: ChatHistoryRepository | None = None
  +_artifact_store: ArtifactStore | None = None
  +
  +
  +def get_chat_history_repository() -> ChatHistoryRepository:
  +    """Return the configured singleton history repository."""
  +    global _history_repository
  +    if _history_repository is not None:
  +        return _history_repository
  +
  +    endpoint = os.environ.get("COSMOS_CHAT_ENDPOINT", "").strip()
  +    mode = os.environ.get("CHAT_HISTORY_STORE", "").strip().lower()
  +    if mode == "disabled":
  +        raise ChatHistoryError("Chat history is disabled in this deployment.")
  +    if endpoint:
  +        _history_repository = CosmosChatHistoryRepository(
  +            endpoint,
  +            os.environ.get("COSMOS_CHAT_DATABASE", "planetary-explorer"),
  +            os.environ.get("COSMOS_CHAT_CONTAINER", "chat-history"),
  +        )
  +    elif mode == "cosmos":
  +        raise ChatHistoryError("COSMOS_CHAT_ENDPOINT is required for Cosmos history.")
  +    else:
  +        _history_repository = InMemoryChatHistoryRepository()
  +    return _history_repository
  +
  +
  +def get_artifact_store() -> ArtifactStore:
  +    """Return the configured singleton attachment store."""
  +    global _artifact_store
  +    if _artifact_store is not None:
  +        return _artifact_store
  +
  +    endpoint = os.environ.get("CHAT_ARTIFACT_BLOB_ENDPOINT", "").strip()
  +    mode = os.environ.get("CHAT_ARTIFACT_STORE", "").strip().lower()
  +    if mode == "disabled":
  +        raise ChatHistoryError("Chat artifacts are disabled in this deployment.")
  +    if endpoint:
  +        _artifact_store = BlobArtifactStore(
  +            endpoint,
  +            os.environ.get("CHAT_ARTIFACT_CONTAINER", "chat-artifacts"),
  +        )
  +    elif mode == "blob":
  +        raise ChatHistoryError(
  +            "CHAT_ARTIFACT_BLOB_ENDPOINT is required for Blob artifacts."
  +        )
  +    else:
  +        _artifact_store = InMemoryArtifactStore()
  +    return _artifact_store
  \ No newline at end of file
  diff --git a/planetary-explorer/container-app/fastapi_app.py b/planetary-explorer/container-app/fastapi_app.py
  index 093bef1..bcc08d9 100644
  --- a/planetary-explorer/container-app/fastapi_app.py
  +++ b/planetary-explorer/container-app/fastapi_app.py
  @@ -349,6 +349,10 @@ logger = logging.getLogger(__name__)
   # Initialize FastAPI app
   app = FastAPI(title="Planetary Explorer API", version="1.0.0")
   
  +from chat_history_api import router as chat_history_router
  +
  +app.include_router(chat_history_router)
  +
   # Configure CORS origins from environment variable
   cors_origins_str = os.environ.get("CORS_ORIGINS", "http://localhost:5173")
   cors_origin_tokens = [
  @@ -2682,6 +2686,7 @@ async def get_config():
               "mpcPublic": True,
               "mpcPro": _env_flag("PE_FEATURE_MPC_PRO", default=False),
               "fabric": _env_flag("PE_FEATURE_FABRIC", default=False),
  +            "chatHistory": _env_flag("PE_FEATURE_CHAT_HISTORY", default=False),
               # Forecast agent is "available" when at least one weather
               # provider endpoint is configured (real Foundry endpoint or
               # the CPU weather stub). With none of these set the
  diff --git a/planetary-explorer/container-app/requirements.txt b/planetary-explorer/container-app/requirements.txt
  index 482c63c..daac854 100644
  --- a/planetary-explorer/container-app/requirements.txt
  +++ b/planetary-explorer/container-app/requirements.txt
  @@ -18,6 +18,8 @@ azure-identity>=1.15.0
   azure-core>=1.29.0
   azure-ai-projects>=1.0.0
   azure-ai-agents>=1.1.0
  +azure-cosmos>=4.9.0,<5.0.0
  +azure-storage-blob>=12.24.0,<13.0.0
   
   # Microsoft Agent Framework ΓÇö CORE ONLY (no [all] extras).
   # Used by the ClarifierDecider Executor (agents/clarifier_agent/Executors/) so
  diff --git a/planetary-explorer/container-app/tests/test_chat_history_api.py b/planetary-explorer/container-app/tests/test_chat_history_api.py
  new file mode 100644
  index 0000000..8b34905
  --- /dev/null
  +++ b/planetary-explorer/container-app/tests/test_chat_history_api.py
  @@ -0,0 +1,239 @@
  +"""Tests for authenticated chat-history and test-bundle endpoints."""
  +
  +import io
  +import json
  +from zipfile import ZipFile
  +
  +from fastapi import FastAPI, Request
  +from fastapi.testclient import TestClient
  +
  +from chat_history_api import router
  +import chat_history_api
  +from chat_history_store import InMemoryArtifactStore, InMemoryChatHistoryRepository
  +
  +
  +def _create_client(
  +    artifacts: InMemoryArtifactStore | None = None,
  +) -> tuple[TestClient, InMemoryChatHistoryRepository]:
  +    repository = InMemoryChatHistoryRepository()
  +    artifact_store = artifacts or InMemoryArtifactStore()
  +    app = FastAPI()
  +
  +    @app.middleware("http")
  +    async def add_test_user(request: Request, call_next):
  +        request.state.user = {
  +            "tid": "tenant-1",
  +            "sub": request.headers.get("X-Test-User", "user-1"),
  +        }
  +        return await call_next(request)
  +
  +    app.include_router(router)
  +    from chat_history_api import get_artifact_store, get_chat_history_repository
  +
  +    app.dependency_overrides[get_chat_history_repository] = lambda: repository
  +    app.dependency_overrides[get_artifact_store] = lambda: artifact_store
  +    return TestClient(app), repository
  +
  +
  +def test_given_saved_session_when_another_user_reads_then_returns_not_found() -> None:
  +    # Arrange
  +    client, _repository = _create_client()
  +    payload = {
  +        "expectedRevision": 0,
  +        "mutationId": "isolation-save",
  +        "messages": [{"role": "user", "content": "Show Seattle"}],
  +    }
  +    saved = client.put("/api/chat-history/sessions/web-session-1", json=payload)
  +
  +    # Act
  +    other_user = client.get(
  +        "/api/chat-history/sessions/web-session-1",
  +        headers={"X-Test-User": "user-2"},
  +    )
  +
  +    # Assert
  +    assert saved.status_code == 200
  +    assert other_user.status_code == 404
  +
  +
  +def test_given_session_with_file_when_exported_then_bundle_is_replayable() -> None:
  +    # Arrange
  +    client, _repository = _create_client()
  +    client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 0,
  +            "mutationId": "export-save",
  +            "messages": [
  +                {"role": "user", "content": "Test these coordinates"},
  +                {"role": "assistant", "content": "Loaded 1 row"},
  +            ],
  +            "context": {"selectedModel": "gpt-5", "selectedModule": "terrain"},
  +        },
  +    )
  +    upload = client.post(
  +        "/api/chat-history/sessions/web-session-1/files",
  +        files={"file": ("coordinates.csv", b"lat,lng\n47.6,-122.3\n", "text/csv")},
  +    )
  +
  +    # Act
  +    exported = client.get("/api/chat-history/sessions/web-session-1/export")
  +    with ZipFile(io.BytesIO(exported.content)) as bundle:
  +        names = set(bundle.namelist())
  +        session = json.loads(bundle.read("session.json"))
  +        data = bundle.read("files/coordinates.csv")
  +
  +    # Assert
  +    assert upload.status_code == 201
  +    assert exported.status_code == 200
  +    assert {"session.json", "README.txt", "files/coordinates.csv"} <= names
  +    assert session["messageCount"] == 2
  +    assert "ownerId" not in session
  +    assert "blobName" not in session["attachments"][0]
  +    assert data == b"lat,lng\n47.6,-122.3\n"
  +
  +
  +def test_given_blob_cleanup_failure_when_deleting_then_session_remains_retryable() -> None:
  +    # Arrange
  +    class FailingArtifactStore(InMemoryArtifactStore):
  +        async def delete(self, attachment: dict) -> None:
  +            raise RuntimeError("storage unavailable")
  +
  +    client, repository = _create_client(FailingArtifactStore())
  +    client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 0,
  +            "mutationId": "session-delete-save",
  +            "messages": [{"role": "user", "content": "keep me"}],
  +        },
  +    )
  +    client.post(
  +        "/api/chat-history/sessions/web-session-1/files",
  +        files={"file": ("coordinates.csv", b"lat,lng\n", "text/csv")},
  +    )
  +
  +    # Act
  +    deleted = client.delete("/api/chat-history/sessions/web-session-1")
  +
  +    # Assert
  +    assert deleted.status_code == 503
  +    assert client.get("/api/chat-history/sessions/web-session-1").status_code == 200
  +
  +
  +def test_given_blob_delete_failure_when_deleting_file_then_metadata_remains_retryable() -> None:
  +    # Arrange
  +    class FailingArtifactStore(InMemoryArtifactStore):
  +        async def delete(self, attachment: dict) -> None:
  +            raise RuntimeError("storage unavailable")
  +
  +    client, _repository = _create_client(FailingArtifactStore())
  +    client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 0,
  +            "mutationId": "file-delete-save",
  +            "messages": [{"role": "user", "content": "keep file metadata"}],
  +        },
  +    )
  +    uploaded = client.post(
  +        "/api/chat-history/sessions/web-session-1/files",
  +        files={"file": ("coordinates.csv", b"lat,lng\n", "text/csv")},
  +    ).json()
  +
  +    # Act
  +    deleted = client.delete(
  +        f"/api/chat-history/sessions/web-session-1/files/{uploaded['id']}"
  +    )
  +
  +    # Assert
  +    assert deleted.status_code == 503
  +    session = client.get("/api/chat-history/sessions/web-session-1").json()
  +    assert session["attachments"][0]["id"] == uploaded["id"]
  +
  +
  +def test_given_active_session_with_file_when_saved_then_attachment_is_renewed() -> None:
  +    # Arrange
  +    class TrackingArtifactStore(InMemoryArtifactStore):
  +        def __init__(self) -> None:
  +            super().__init__()
  +            self.touched: list[str] = []
  +
  +        async def touch(self, attachment: dict) -> None:
  +            await super().touch(attachment)
  +            self.touched.append(attachment["id"])
  +
  +    artifacts = TrackingArtifactStore()
  +    client, _repository = _create_client(artifacts)
  +    client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 0,
  +            "mutationId": "renew-initial-save",
  +            "messages": [{"role": "user", "content": "initial"}],
  +        },
  +    )
  +    uploaded = client.post(
  +        "/api/chat-history/sessions/web-session-1/files",
  +        files={"file": ("coordinates.csv", b"lat,lng\n", "text/csv")},
  +    ).json()
  +
  +    # Act
  +    saved = client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 1,
  +            "mutationId": "renew-continuation-save",
  +            "messages": [{"role": "user", "content": "continued"}],
  +        },
  +    )
  +
  +    # Assert
  +    assert saved.status_code == 200
  +    assert artifacts.touched == [uploaded["id"]]
  +
  +
  +def test_given_committed_mutation_when_retried_then_returns_same_revision() -> None:
  +    # Arrange
  +    client, _repository = _create_client()
  +    payload = {
  +        "expectedRevision": 0,
  +        "mutationId": "ambiguous-success-save",
  +        "messages": [{"role": "user", "content": "save exactly once"}],
  +    }
  +
  +    # Act
  +    first = client.put("/api/chat-history/sessions/web-session-1", json=payload)
  +    replay = client.put("/api/chat-history/sessions/web-session-1", json=payload)
  +
  +    # Assert
  +    assert first.status_code == 200
  +    assert replay.status_code == 200
  +    assert replay.json()["revision"] == first.json()["revision"] == 1
  +
  +
  +def test_given_oversized_attachments_when_exporting_then_rejects_before_download(
  +    monkeypatch,
  +) -> None:
  +    # Arrange
  +    monkeypatch.setattr(chat_history_api, "MAX_EXPORT_BYTES", 10)
  +    client, _repository = _create_client()
  +    client.put(
  +        "/api/chat-history/sessions/web-session-1",
  +        json={
  +            "expectedRevision": 0,
  +            "mutationId": "oversized-export-save",
  +            "messages": [{"role": "user", "content": "large export"}],
  +        },
  +    )
  +    client.post(
  +        "/api/chat-history/sessions/web-session-1/files",
  +        files={"file": ("coordinates.csv", b"12345678901", "text/csv")},
  +    )
  +
  +    # Act
  +    exported = client.get("/api/chat-history/sessions/web-session-1/export")
  +
  +    # Assert
  +    assert exported.status_code == 413
  +    assert exported.json()["detail"] == "Test bundle exceeds the 100 MB export limit."
  \ No newline at end of file
  diff --git a/planetary-explorer/container-app/tests/test_chat_history_store.py b/planetary-explorer/container-app/tests/test_chat_history_store.py
  new file mode 100644
  index 0000000..55973ef
  --- /dev/null
  +++ b/planetary-explorer/container-app/tests/test_chat_history_store.py
  @@ -0,0 +1,268 @@
  +"""Tests for owner-isolated chat history persistence."""
  +
  +import copy
  +
  +import pytest
  +
  +import chat_history_store
  +from chat_history_store import (
  +    BlobArtifactStore,
  +    ChatHistoryError,
  +    ChatHistoryConflictError,
  +    CosmosChatHistoryRepository,
  +    InMemoryArtifactStore,
  +    InMemoryChatHistoryRepository,
  +    normalize_session_document,
  +    public_session_document,
  +)
  +
  +
  +class _FakeBlob:
  +    def __init__(self) -> None:
  +        self.upload: dict | None = None
  +
  +    async def upload_blob(self, content: bytes, **kwargs) -> None:
  +        self.upload = {"content": content, **kwargs}
  +
  +
  +class _FakeBlobContainer:
  +    def __init__(self) -> None:
  +        self.blob = _FakeBlob()
  +
  +    def get_blob_client(self, _blob_name: str) -> _FakeBlob:
  +        return self.blob
  +
  +
  +class _CosmosStatusError(Exception):
  +    def __init__(self, status_code: int) -> None:
  +        self.status_code = status_code
  +
  +
  +class _ConflictingCosmosContainer:
  +    def __init__(self) -> None:
  +        self.document = normalize_session_document(
  +            "tenant:user-1",
  +            "web-session-1",
  +            {
  +                "expectedRevision": 0,
  +                "mutationId": "cosmos-initial-save",
  +                "messages": [{"role": "user", "content": "old"}],
  +            },
  +        )
  +        self.document["_etag"] = "etag-1"
  +        self.replace_calls = 0
  +
  +    async def read_item(self, *, item: str, partition_key: str) -> dict:
  +        assert item == "web-session-1"
  +        assert partition_key == "tenant:user-1"
  +        return copy.deepcopy(self.document)
  +
  +    async def replace_item(self, *, item: str, body: dict, **_kwargs) -> dict:
  +        self.replace_calls += 1
  +        if self.replace_calls == 1:
  +            self.document["attachments"] = [{"id": "concurrent-file"}]
  +            self.document["_etag"] = "etag-2"
  +            raise _CosmosStatusError(412)
  +        self.document = copy.deepcopy(body)
  +        self.document["_etag"] = "etag-3"
  +        return copy.deepcopy(self.document)
  +
  +
  +@pytest.mark.asyncio
  +async def test_given_same_session_id_when_owners_differ_then_history_is_isolated() -> None:
  +    # Arrange
  +    repository = InMemoryChatHistoryRepository()
  +
  +    # Act
  +    await repository.upsert_session(
  +        "tenant:user-1",
  +        "web-session-1",
  +        {
  +            "expectedRevision": 0,
  +            "mutationId": "owner-one-save",
  +            "messages": [{"role": "user", "content": "first"}],
  +        },
  +    )
  +    await repository.upsert_session(
  +        "tenant:user-2",
  +        "web-session-1",
  +        {
  +            "expectedRevision": 0,
  +            "mutationId": "owner-two-save",
  +            "messages": [{"role": "user", "content": "second"}],
  +        },
  +    )
  +
  +    # Assert
  +    first = await repository.get_session("tenant:user-1", "web-session-1")
  +    second = await repository.get_session("tenant:user-2", "web-session-1")
  +    assert first["messages"][0]["content"] == "first"
  +    assert second["messages"][0]["content"] == "second"
  +
  +
  +def test_given_sensitive_context_when_normalized_then_secrets_and_large_data_are_removed() -> None:
  +    # Arrange
  +    signed_url = "https://example.blob.core.windows.net/file.tif?sv=1&sig=secret"
  +    payload = {
  +        "expectedRevision": 0,
  +        "mutationId": "sanitization-save",
  +        "messages": [
  +            {
  +                "role": "assistant",
  +                "content": f"Download {signed_url}",
  +                "toolTrace": [{"args": {
  +                    "accessToken": "secret",
  +                    "authorization": "Bearer abc.def.ghi",
  +                    "artifact_uri": signed_url,
  +                }}],
  +            }
  +        ],
  +        "context": {
  +            "selectedModel": "gpt-5",
  +            "map": {
  +                "imagery_base64": "a" * 1000,
  +                "stac_items": [{"assets": {"data": {"href": signed_url}}}],
  +                "bounds": {"north": 1},
  +            },
  +        },
  +    }
  +
  +    # Act
  +    document = normalize_session_document("tenant:user-1", "web-session-1", payload)
  +    public = public_session_document(document)
  +
  +    # Assert
  +    assert public["messages"][0]["content"].endswith("?<redacted>")
  +    assert "accessToken" not in public["messages"][0]["toolTrace"][0]["args"]
  +    assert "authorization" not in public["messages"][0]["toolTrace"][0]["args"]
  +    assert public["context"]["map"] == {"bounds": {"north": 1}}
  +    assert "ownerId" not in public
  +
  +
  +@pytest.mark.asyncio
  +async def test_given_data_file_when_uploaded_then_manifest_and_bytes_round_trip() -> None:
  +    # Arrange
  +    store = InMemoryArtifactStore()
  +    content = b"latitude,longitude\n47.6,-122.3\n"
  +
  +    # Act
  +    attachment = await store.upload(
  +        "tenant:user-1",
  +        "web-session-1",
  +        "test-data.csv",
  +        "text/csv",
  +        content,
  +    )
  +    downloaded = await store.download(attachment)
  +
  +    # Assert
  +    assert attachment["name"] == "test-data.csv"
  +    assert attachment["size"] == len(content)
  +    assert downloaded == content
  +
  +
  +@pytest.mark.asyncio
  +async def test_given_blob_upload_when_content_type_supplied_then_uses_content_settings() -> None:
  +    # Arrange
  +    container = _FakeBlobContainer()
  +    store = BlobArtifactStore(
  +        "https://storage.example",
  +        "chat-artifacts",
  +        container_client=container,
  +    )
  +
  +    # Act
  +    await store.upload(
  +        "tenant:user-1",
  +        "web-session-1",
  +        "coordinates.csv",
  +        "text/csv",
  +        b"lat,lng\n",
  +    )
  +
  +    # Assert
  +    assert container.blob.upload is not None
  +    assert container.blob.upload["content_settings"].content_type == "text/csv"
  +
  +
  +@pytest.mark.asyncio
  +async def test_given_cosmos_etag_conflict_when_saving_then_retries_without_losing_attachments() -> None:
  +    # Arrange
  +    container = _ConflictingCosmosContainer()
  +    repository = CosmosChatHistoryRepository(
  +        "https://cosmos.example",
  +        "planetary-explorer",
  +        "chat-history",
  +        container_client=container,
  +    )
  +
  +    # Act
  +    saved = await repository.upsert_session(
  +        "tenant:user-1",
  +        "web-session-1",
  +        {
  +            "expectedRevision": 1,
  +            "mutationId": "cosmos-updated-save",
  +            "messages": [{"role": "user", "content": "new"}],
  +        },
  +    )
  +
  +    # Assert
  +    assert container.replace_calls == 2
  +    assert saved["messages"][0]["content"] == "new"
  +    assert saved["attachments"] == [{"id": "concurrent-file"}]
  +
  +
  +def test_given_disabled_modes_with_endpoints_when_resolved_then_stores_remain_disabled(
  +    monkeypatch,
  +) -> None:
  +    # Arrange
  +    monkeypatch.setattr(chat_history_store, "_history_repository", None)
  +    monkeypatch.setattr(chat_history_store, "_artifact_store", None)
  +    monkeypatch.setenv("CHAT_HISTORY_STORE", "disabled")
  +    monkeypatch.setenv("COSMOS_CHAT_ENDPOINT", "https://cosmos.example")
  +    monkeypatch.setenv("CHAT_ARTIFACT_STORE", "disabled")
  +    monkeypatch.setenv("CHAT_ARTIFACT_BLOB_ENDPOINT", "https://blob.example")
  +
  +    # Act & Assert
  +    with pytest.raises(ChatHistoryError, match="history is disabled"):
  +        chat_history_store.get_chat_history_repository()
  +    with pytest.raises(ChatHistoryError, match="artifacts are disabled"):
  +        chat_history_store.get_artifact_store()
  +
  +
  +@pytest.mark.asyncio
  +async def test_given_session_deletion_tombstone_when_write_arrives_then_write_is_rejected() -> None:
  +    # Arrange
  +    repository = InMemoryChatHistoryRepository()
  +    document = await repository.upsert_session(
  +        "tenant:user-1",
  +        "web-session-1",
  +        {
  +            "expectedRevision": 0,
  +            "mutationId": "tombstone-initial-save",
  +            "messages": [{"role": "user", "content": "original"}],
  +        },
  +    )
  +    deleting = await repository.begin_delete(
  +        "tenant:user-1",
  +        "web-session-1",
  +        document["_etag"],
  +    )
  +
  +    # Act & Assert
  +    with pytest.raises(ChatHistoryConflictError, match="being deleted"):
  +        await repository.upsert_session(
  +            "tenant:user-1",
  +            "web-session-1",
  +            {
  +                "expectedRevision": 1,
  +                "mutationId": "tombstone-late-save",
  +                "messages": [{"role": "user", "content": "late write"}],
  +            },
  +        )
  +    await repository.delete_session(
  +        "tenant:user-1",
  +        "web-session-1",
  +        deleting["_etag"],
  +    )
  \ No newline at end of file
  diff --git a/planetary-explorer/infra/app/web.bicep b/planetary-explorer/infra/app/web.bicep
  index 41dfd93..77fc025 100644
  --- a/planetary-explorer/infra/app/web.bicep
  +++ b/planetary-explorer/infra/app/web.bicep
  @@ -61,6 +61,24 @@ param microsoftEntraClientId string = ''
   @secure()
   param microsoftEntraClientSecret string = ''
   
  +@description('Enable durable per-user chat history and downloadable test artifacts.')
  +param enableChatHistory bool = false
  +
  +@description('Keyless Cosmos DB endpoint for chat history.')
  +param cosmosChatEndpoint string = ''
  +
  +@description('Cosmos DB database containing chat sessions.')
  +param cosmosChatDatabase string = 'planetary-explorer'
  +
  +@description('Cosmos DB container containing user-partitioned chat sessions.')
  +param cosmosChatContainer string = 'chat-history'
  +
  +@description('Blob service endpoint for private chat artifact files.')
  +param chatArtifactBlobEndpoint string = ''
  +
  +@description('Private Blob container containing chat artifact files.')
  +param chatArtifactContainer string = 'chat-artifacts'
  +
   // Cloud environment
   @description('Cloud environment: Commercial or Government')
   @allowed(['Commercial', 'Government'])
  @@ -87,6 +105,12 @@ param geoFmMcpApiKey string = ''
   @description('HMAC key used to sign authenticated GeoFM run-owner operations.')
   param geoFmOwnerSigningKey string = ''
   
  +@description('Internal URL of the Azure Web Search MCP service. Empty disables current-web tools.')
  +param webSearchMcpUrl string = ''
  +
  +@description('Enable current-date and web-search MCP tools in the AnalystAgent.')
  +param enableWebSearch bool = false
  +
   // UI feature flags surfaced via /api/config so the frontend can show or
   // lock controls without redeploying the bundle. These are independent of
   // the deploy* flags on the infra side ΓÇö e.g. an operator can deploy the
  @@ -263,6 +287,38 @@ resource app 'Microsoft.App/containerApps@2023-05-01' = {
                 name: 'AZURE_AD_CLIENT_ID'
                 value: microsoftEntraClientId
               }
  +            {
  +              name: 'PE_FEATURE_CHAT_HISTORY'
  +              value: enableChatHistory ? 'true' : 'false'
  +            }
  +            {
  +              name: 'CHAT_HISTORY_STORE'
  +              value: enableChatHistory ? 'cosmos' : 'disabled'
  +            }
  +            {
  +              name: 'COSMOS_CHAT_ENDPOINT'
  +              value: cosmosChatEndpoint
  +            }
  +            {
  +              name: 'COSMOS_CHAT_DATABASE'
  +              value: cosmosChatDatabase
  +            }
  +            {
  +              name: 'COSMOS_CHAT_CONTAINER'
  +              value: cosmosChatContainer
  +            }
  +            {
  +              name: 'CHAT_ARTIFACT_STORE'
  +              value: enableChatHistory ? 'blob' : 'disabled'
  +            }
  +            {
  +              name: 'CHAT_ARTIFACT_BLOB_ENDPOINT'
  +              value: chatArtifactBlobEndpoint
  +            }
  +            {
  +              name: 'CHAT_ARTIFACT_CONTAINER'
  +              value: chatArtifactContainer
  +            }
               {
                 name: 'AZURE_OPENAI_ENDPOINT'
                 value: azureOpenAiEndpoint
  @@ -318,6 +374,14 @@ resource app 'Microsoft.App/containerApps@2023-05-01' = {
                 name: 'GEOFM_MCP_URL'
                 value: geoFmMcpUrl
               }
  +            {
  +              name: 'WEB_SEARCH_ENABLED'
  +              value: enableWebSearch ? 'true' : 'false'
  +            }
  +            {
  +              name: 'WEB_SEARCH_MCP_URL'
  +              value: webSearchMcpUrl
  +            }
               {
                 // UI feature flags. Read by the backend's /api/config
                 // endpoint and reflected to the SPA so it can lock controls
  diff --git a/planetary-explorer/infra/main.bicep b/planetary-explorer/infra/main.bicep
  index fefad9b..d71fe4c 100644
  --- a/planetary-explorer/infra/main.bicep
  +++ b/planetary-explorer/infra/main.bicep
  @@ -51,6 +51,9 @@ param enableAuthentication bool = false
   @description('Explicitly expose protected API routes without authentication for development or demos.')
   param publicDemoMode bool = false
   
  +@description('Deploy durable per-user chat history in Cosmos DB with downloadable artifacts in Blob Storage.')
  +param deployChatHistory bool = true
  +
   @description('Microsoft Entra Client ID (Application ID)')
   param microsoftEntraClientId string = ''
   
  @@ -352,7 +355,7 @@ module appsEnv './shared/apps-env.bicep' = if (shouldDeployAppsEnvironment) {
   }
   
   // Storage Account (required for AI Foundry Hub)
  -module storage './shared/storage.bicep' = if (deployAIFoundry || deployGeoFm) {
  +module storage './shared/storage.bicep' = if (deployAIFoundry || deployGeoFm || deployChatHistory) {
     name: 'storage'
     scope: rg
     params: {
  @@ -361,6 +364,18 @@ module storage './shared/storage.bicep' = if (deployAIFoundry || deployGeoFm) {
       tags: tags
       enablePrivateEndpoints: enablePrivateEndpoints
       deployGeoFmResources: deployGeoFm
  +    deployChatHistoryResources: deployChatHistory
  +  }
  +}
  +
  +module chatHistory './shared/cosmos-chat-history.bicep' = if (deployChatHistory) {
  +  name: 'chat-history'
  +  scope: rg
  +  params: {
  +    name: '${abbrs.documentDBDatabaseAccounts}${resourceToken}'
  +    location: location
  +    tags: tags
  +    enablePrivateEndpoints: enablePrivateEndpoints
     }
   }
   
  @@ -446,7 +461,7 @@ module peKeyVault './shared/private-endpoint.bicep' = if (enablePrivateEndpoints
     }
   }
   
  -module peStorageBlob './shared/private-endpoint.bicep' = if (enablePrivateEndpoints && (deployAIFoundry || deployGeoFm)) {
  +module peStorageBlob './shared/private-endpoint.bicep' = if (enablePrivateEndpoints && (deployAIFoundry || deployGeoFm || deployChatHistory)) {
     name: 'pe-storage-blob'
     scope: rg
     params: {
  @@ -460,6 +475,20 @@ module peStorageBlob './shared/private-endpoint.bicep' = if (enablePrivateEndpoi
     }
   }
   
  +module peChatHistory './shared/private-endpoint.bicep' = if (enablePrivateEndpoints && deployChatHistory) {
  +  name: 'pe-chat-history'
  +  scope: rg
  +  params: {
  +    name: 'pe-cosmos-${resourceToken}'
  +    location: location
  +    tags: tags
  +    serviceResourceId: chatHistory.?outputs.?accountId ?? ''
  +    groupId: 'Sql'
  +    subnetId: networking.?outputs.?privateEndpointsSubnetId ?? ''
  +    privateDnsZoneId: privateDnsZones.?outputs.?cosmosSqlDnsZoneId ?? ''
  +  }
  +}
  +
   module peStorageQueue './shared/private-endpoint.bicep' = if (enablePrivateEndpoints && deployGeoFm) {
     name: 'pe-storage-queue'
     scope: rg
  @@ -601,6 +630,12 @@ module web './app/web.bicep' = if (shouldDeployApiContainer) {
       microsoftEntraClientId: microsoftEntraClientId
       microsoftEntraTenantId: microsoftEntraTenantId
       microsoftEntraClientSecret: microsoftEntraClientSecret
  +    enableChatHistory: deployChatHistory && !publicDemoMode
  +    cosmosChatEndpoint: deployChatHistory && !publicDemoMode ? (chatHistory.?outputs.?endpoint ?? '') : ''
  +    cosmosChatDatabase: chatHistory.?outputs.?databaseName ?? 'planetary-explorer'
  +    cosmosChatContainer: chatHistory.?outputs.?containerName ?? 'chat-history'
  +    chatArtifactBlobEndpoint: deployChatHistory && !publicDemoMode ? (storage.?outputs.?blobEndpoint ?? '') : ''
  +    chatArtifactContainer: storage.?outputs.?chatArtifactContainerName ?? 'chat-artifacts'
       // Cloud environment
       cloudEnvironment: cloudEnvironment
       // AI Agent Service project endpoint
  @@ -641,6 +676,19 @@ module web './app/web.bicep' = if (shouldDeployApiContainer) {
     }
   }
   
  +module chatHistoryAccess './shared/chat-history-access.bicep' = if (deployChatHistory && shouldDeployApiContainer && !publicDemoMode) {
  +  name: 'chat-history-access'
  +  scope: rg
  +  params: {
  +    cosmosAccountName: chatHistory.?outputs.?accountName ?? ''
  +    cosmosDatabaseName: chatHistory.?outputs.?databaseName ?? ''
  +    cosmosContainerName: chatHistory.?outputs.?containerName ?? ''
  +    storageAccountName: storage.?outputs.?name ?? ''
  +    blobContainerName: storage.?outputs.?chatArtifactContainerName ?? ''
  +    principalId: web.?outputs.?principalId ?? ''
  +  }
  +}
  +
   // Forecast Agent provider URLs + KV-User role assignment for the API
   // container. Wraps RG-scope resources in a module so they deploy from
   // this subscription-scope template.
  @@ -756,6 +804,7 @@ output AZURE_CONTAINER_REGISTRY_NAME string = registry.outputs.name
   output AZURE_CONTAINER_APPS_ENVIRONMENT_NAME string = appsEnv.?outputs.?name ?? resolvedContainerAppsEnvironmentName
   output AZURE_CONTAINER_APP_NAME string = shouldDeployApiContainer ? (web.?outputs.?name ?? '') : apiContainerAppName
   output AZURE_CONTAINER_APP_URL string = shouldDeployApiContainer ? (web.?outputs.?uri ?? '') : existingApiUrl
  +output AZURE_COSMOS_CHAT_HISTORY_ENDPOINT string = chatHistory.?outputs.?endpoint ?? ''
   output AZURE_WEB_APP_NAME string = deployFrontend ? (frontend.?outputs.?webAppName ?? '') : frontendWebAppName
   output AZURE_WEB_APP_URL string = deployFrontend ? (frontend.?outputs.?webAppUrl ?? '') : resolvedFrontendUrl
   output AZURE_APP_SERVICE_PLAN_NAME string = deployFrontend ? (frontend.?outputs.?appServicePlanName ?? '') : frontendAppServicePlanName
  diff --git a/planetary-explorer/infra/shared/chat-history-access.bicep b/planetary-explorer/infra/shared/chat-history-access.bicep
  new file mode 100644
  index 0000000..5115dcc
  --- /dev/null
  +++ b/planetary-explorer/infra/shared/chat-history-access.bicep
  @@ -0,0 +1,64 @@
  +metadata name = 'Chat History Data Access'
  +metadata description = 'Grants the API managed identity access to chat history and artifact data.'
  +
  +@description('Name of the Cosmos DB account containing chat history.')
  +param cosmosAccountName string
  +
  +@description('Name of the Cosmos DB for NoSQL database.')
  +param cosmosDatabaseName string
  +
  +@description('Name of the Cosmos DB chat history container.')
  +param cosmosContainerName string
  +
  +@description('Name of the storage account containing chat artifacts.')
  +param storageAccountName string
  +
  +@description('Name of the private Blob container containing chat artifacts.')
  +param blobContainerName string
  +
  +@description('Object ID of the API Container App managed identity.')
  +param principalId string
  +
  +resource cosmosAccount 'Microsoft.DocumentDB/databaseAccounts@2024-11-15' existing = {
  +  name: cosmosAccountName
  +}
  +
  +resource storageAccount 'Microsoft.Storage/storageAccounts@2023-01-01' existing = {
  +  name: storageAccountName
  +}
  +
  +resource blobService 'Microsoft.Storage/storageAccounts/blobServices@2023-01-01' existing = {
  +  parent: storageAccount
  +  name: 'default'
  +}
  +
  +resource artifactContainer 'Microsoft.Storage/storageAccounts/blobServices/containers@2023-01-01' existing = {
  +  parent: blobService
  +  name: blobContainerName
  +}
  +
  +var cosmosDataContributorRole = '00000000-0000-0000-0000-000000000002'
  +var storageBlobDataContributorRole = 'ba92f5b4-2d11-453d-a403-e96b0029c9fe'
  +
  +resource cosmosDataAccess 'Microsoft.DocumentDB/databaseAccounts/sqlRoleAssignments@2024-11-15' = if (!empty(principalId)) {
  +  parent: cosmosAccount
  +  name: guid(cosmosAccount.id, principalId, cosmosDataContributorRole, cosmosContainerName)
  +  properties: {
  +    principalId: principalId
  +    roleDefinitionId: '${cosmosAccount.id}/sqlRoleDefinitions/${cosmosDataContributorRole}'
  +    scope: '${cosmosAccount.id}/dbs/${cosmosDatabaseName}/colls/${cosmosContainerName}'
  +  }
  +}
  +
  +resource blobDataAccess 'Microsoft.Authorization/roleAssignments@2022-04-01' = if (!empty(principalId)) {
  +  name: guid(artifactContainer.id, principalId, storageBlobDataContributorRole)
  +  scope: artifactContainer
  +  properties: {
  +    principalId: principalId
  +    principalType: 'ServicePrincipal'
  +    roleDefinitionId: subscriptionResourceId(
  +      'Microsoft.Authorization/roleDefinitions',
  +      storageBlobDataContributorRole
  +    )
  +  }
  +}
  \ No newline at end of file
  diff --git a/planetary-explorer/infra/shared/cosmos-chat-history.bicep b/planetary-explorer/infra/shared/cosmos-chat-history.bicep
  new file mode 100644
  index 0000000..68ea1aa
  --- /dev/null
  +++ b/planetary-explorer/infra/shared/cosmos-chat-history.bicep
  @@ -0,0 +1,119 @@
  +metadata name = 'Chat History Cosmos DB'
  +metadata description = 'Deploys a serverless Cosmos DB for user-partitioned chat history.'
  +
  +@description('Name of the Azure Cosmos DB account.')
  +param name string
  +
  +@description('Azure region for the Cosmos DB account.')
  +param location string = resourceGroup().location
  +
  +@description('Resource tags applied to the Cosmos DB account.')
  +param tags object = {}
  +
  +@description('Name of the Cosmos DB for NoSQL database.')
  +param databaseName string = 'planetary-explorer'
  +
  +@description('Name of the user-partitioned chat history container.')
  +param containerName string = 'chat-history'
  +
  +@description('Number of days saved chat sessions remain available.')
  +@minValue(1)
  +param retentionDays int = 90
  +
  +@description('Disable public network access when private endpoints are enabled.')
  +param enablePrivateEndpoints bool = false
  +
  +resource account 'Microsoft.DocumentDB/databaseAccounts@2024-11-15' = {
  +  name: name
  +  location: location
  +  tags: tags
  +  kind: 'GlobalDocumentDB'
  +  properties: {
  +    databaseAccountOfferType: 'Standard'
  +    capabilities: [
  +      {
  +        name: 'EnableServerless'
  +      }
  +    ]
  +    consistencyPolicy: {
  +      defaultConsistencyLevel: 'Session'
  +    }
  +    disableKeyBasedMetadataWriteAccess: true
  +    disableLocalAuth: true
  +    enableAutomaticFailover: false
  +    enableMultipleWriteLocations: false
  +    locations: [
  +      {
  +        locationName: location
  +        failoverPriority: 0
  +        isZoneRedundant: false
  +      }
  +    ]
  +    minimalTlsVersion: 'Tls12'
  +    networkAclBypass: 'AzureServices'
  +    publicNetworkAccess: enablePrivateEndpoints ? 'Disabled' : 'Enabled'
  +  }
  +}
  +
  +resource database 'Microsoft.DocumentDB/databaseAccounts/sqlDatabases@2024-11-15' = {
  +  parent: account
  +  name: databaseName
  +  properties: {
  +    resource: {
  +      id: databaseName
  +    }
  +  }
  +}
  +
  +resource historyContainer 'Microsoft.DocumentDB/databaseAccounts/sqlDatabases/containers@2024-11-15' = {
  +  parent: database
  +  name: containerName
  +  properties: {
  +    resource: {
  +      id: containerName
  +      defaultTtl: retentionDays * 86400
  +      partitionKey: {
  +        paths: [
  +          '/ownerId'
  +        ]
  +        kind: 'Hash'
  +        version: 2
  +      }
  +      indexingPolicy: {
  +        automatic: true
  +        indexingMode: 'consistent'
  +        includedPaths: [
  +          {
  +            path: '/*'
  +          }
  +        ]
  +        excludedPaths: [
  +          {
  +            path: '/messages/*'
  +          }
  +          {
  +            path: '/context/*'
  +          }
  +          {
  +            path: '/attachments/*'
  +          }
  +        ]
  +      }
  +    }
  +  }
  +}
  +
  +@description('Name of the deployed Cosmos DB account.')
  +output accountName string = account.name
  +
  +@description('Resource ID of the deployed Cosmos DB account.')
  +output accountId string = account.id
  +
  +@description('Keyless Cosmos DB for NoSQL endpoint used by the API.')
  +output endpoint string = account.properties.documentEndpoint
  +
  +@description('Name of the chat history database.')
  +output databaseName string = database.name
  +
  +@description('Name of the chat history container.')
  +output containerName string = historyContainer.name
  \ No newline at end of file
  diff --git a/planetary-explorer/infra/shared/private-dns-zones.bicep b/planetary-explorer/infra/shared/private-dns-zones.bicep
  index de2579c..0ff1308 100644
  --- a/planetary-explorer/infra/shared/private-dns-zones.bicep
  +++ b/planetary-explorer/infra/shared/private-dns-zones.bicep
  @@ -11,6 +11,7 @@ param cloudEnvironment string = 'Commercial'
   
   // DNS zone names differ between Commercial and Government clouds
   var dnsZones = cloudEnvironment == 'Government' ? {
  +  cosmosSql: 'privatelink.documents.azure.us'
     keyVault: 'privatelink.vaultcore.usgovcloudapi.net'
     storageBlob: 'privatelink.blob.core.usgovcloudapi.net'
     storageFile: 'privatelink.file.core.usgovcloudapi.net'
  @@ -23,6 +24,7 @@ var dnsZones = cloudEnvironment == 'Government' ? {
     mlNotebooks: 'privatelink.notebooks.usgovcloudapi.net'
     servicesAi: 'privatelink.services.ai.azure.us'
   } : {
  +  cosmosSql: 'privatelink.documents.azure.com'
     keyVault: 'privatelink.vaultcore.azure.net'
     #disable-next-line no-hardcoded-env-urls // Private DNS zone names must be exact strings
     storageBlob: 'privatelink.blob.core.windows.net'
  @@ -39,6 +41,22 @@ var dnsZones = cloudEnvironment == 'Government' ? {
     servicesAi: 'privatelink.services.ai.azure.com'
   }
   
  +// ΓöÇΓöÇ Cosmos DB for NoSQL ΓöÇΓöÇ
  +resource dnsZoneCosmosSql 'Microsoft.Network/privateDnsZones@2024-06-01' = {
  +  name: dnsZones.cosmosSql
  +  location: 'global'
  +  tags: tags
  +}
  +resource dnsZoneCosmosSqlLink 'Microsoft.Network/privateDnsZones/virtualNetworkLinks@2024-06-01' = {
  +  parent: dnsZoneCosmosSql
  +  name: 'link-cosmos-sql'
  +  location: 'global'
  +  properties: {
  +    virtualNetwork: { id: vnetId }
  +    registrationEnabled: false
  +  }
  +}
  +
   // ΓöÇΓöÇ Key Vault ΓöÇΓöÇ
   resource dnsZoneKeyVault 'Microsoft.Network/privateDnsZones@2024-06-01' = {
     name: dnsZones.keyVault
  @@ -220,6 +238,7 @@ resource dnsZoneServicesAiLink 'Microsoft.Network/privateDnsZones/virtualNetwork
   }
   
   // Outputs ΓÇö DNS zone IDs needed when creating private endpoints
  +output cosmosSqlDnsZoneId string = dnsZoneCosmosSql.id
   output keyVaultDnsZoneId string = dnsZoneKeyVault.id
   output storageBlobDnsZoneId string = dnsZoneStorageBlob.id
   output storageFileDnsZoneId string = dnsZoneStorageFile.id
  diff --git a/planetary-explorer/infra/shared/storage.bicep b/planetary-explorer/infra/shared/storage.bicep
  index 2828253..427c654 100644
  --- a/planetary-explorer/infra/shared/storage.bicep
  +++ b/planetary-explorer/infra/shared/storage.bicep
  @@ -12,6 +12,13 @@ param enablePrivateEndpoints bool = false
   @description('Create the private Blob container and Queue used by durable GeoFM runs.')
   param deployGeoFmResources bool = false
   
  +@description('Create the private Blob container used by downloadable chat test artifacts.')
  +param deployChatHistoryResources bool = false
  +
  +@description('Number of days chat artifact files remain available.')
  +@minValue(1)
  +param chatArtifactRetentionDays int = 90
  +
   resource storageAccount 'Microsoft.Storage/storageAccounts@2023-01-01' = {
     name: name
     location: location
  @@ -45,6 +52,47 @@ resource dataContainer 'Microsoft.Storage/storageAccounts/blobServices/container
     }
   }
   
  +resource chatArtifactContainer 'Microsoft.Storage/storageAccounts/blobServices/containers@2023-01-01' = if (deployChatHistoryResources) {
  +  parent: blobService
  +  name: 'chat-artifacts'
  +  properties: {
  +    publicAccess: 'None'
  +  }
  +}
  +
  +resource lifecyclePolicy 'Microsoft.Storage/storageAccounts/managementPolicies@2023-01-01' = if (deployChatHistoryResources) {
  +  parent: storageAccount
  +  name: 'default'
  +  properties: {
  +    policy: {
  +      rules: [
  +        {
  +          enabled: true
  +          name: 'expire-chat-artifacts'
  +          type: 'Lifecycle'
  +          definition: {
  +            actions: {
  +              baseBlob: {
  +                delete: {
  +                  daysAfterModificationGreaterThan: chatArtifactRetentionDays
  +                }
  +              }
  +            }
  +            filters: {
  +              blobTypes: [
  +                'blockBlob'
  +              ]
  +              prefixMatch: [
  +                '${chatArtifactContainer.name}/'
  +              ]
  +            }
  +          }
  +        }
  +      ]
  +    }
  +  }
  +}
  +
   resource geoFmContainer 'Microsoft.Storage/storageAccounts/blobServices/containers@2023-01-01' = if (deployGeoFmResources) {
     parent: blobService
     name: 'geofm'
  @@ -76,3 +124,4 @@ output queueEndpoint string = storageAccount.properties.primaryEndpoints.queue
   output geoFmContainerName string = deployGeoFmResources ? geoFmContainer.name : ''
   output geoFmQueueName string = deployGeoFmResources ? geoFmQueue.name : ''
   output geoFmPoisonQueueName string = deployGeoFmResources ? geoFmPoisonQueue.name : ''
  +output chatArtifactContainerName string = deployChatHistoryResources ? chatArtifactContainer.name : ''
  diff --git a/planetary-explorer/web-ui/package-lock.json b/planetary-explorer/web-ui/package-lock.json
  index 3a1f428..54ce70e 100644
  --- a/planetary-explorer/web-ui/package-lock.json
  +++ b/planetary-explorer/web-ui/package-lock.json
  @@ -10,6 +10,7 @@
         "dependencies": {
           "@tanstack/react-query": "^5.51.9",
           "axios": "^1.7.7",
  +        "lucide-react": "^0.468.0",
           "modern-normalize": "^2.0.0",
           "react": "^18.3.1",
           "react-dom": "^18.3.1"
  @@ -2931,6 +2932,15 @@
           "yallist": "^3.0.2"
         }
       },
  +    "node_modules/lucide-react": {
  +      "version": "0.468.0",
  +      "resolved": "https://ms-feed-2.pkgs.visualstudio.com/1es-public/_packaging/npm-public/npm/registry/lucide-react/-/lucide-react-0.468.0.tgz",
  +      "integrity": "sha1-gwwb/ZBVdd3SO4MrqkIMh9sWaRA=",
  +      "license": "ISC",
  +      "peerDependencies": {
  +        "react": "^16.5.1 || ^17.0.0 || ^18.0.0 || ^19.0.0-rc"
  +      }
  +    },
       "node_modules/lz-string": {
         "version": "1.5.0",
         "resolved": "https://registry.npmjs.org/lz-string/-/lz-string-1.5.0.tgz",
  diff --git a/planetary-explorer/web-ui/package.json b/planetary-explorer/web-ui/package.json
  index d9f428a..dcb2f13 100644
  --- a/planetary-explorer/web-ui/package.json
  +++ b/planetary-explorer/web-ui/package.json
  @@ -19,6 +19,7 @@
     "dependencies": {
       "@tanstack/react-query": "^5.51.9",
       "axios": "^1.7.7",
  +    "lucide-react": "^0.468.0",
       "modern-normalize": "^2.0.0",
       "react": "^18.3.1",
       "react-dom": "^18.3.1"
  diff --git a/planetary-explorer/web-ui/src/App.tsx b/planetary-explorer/web-ui/src/App.tsx
  index 3b9f5b3..962884e 100644
  --- a/planetary-explorer/web-ui/src/App.tsx
  +++ b/planetary-explorer/web-ui/src/App.tsx
  @@ -8,6 +8,7 @@ import LandingPage from './components/LandingPage';
   import MainApp from './components/MainApp';
   import { GlobalStyles } from './styles/GlobalStyles';
   import { API_BASE_URL } from './config/api';
  +import type { ChatHistoryContext } from './services/api';
   
   const queryClient = new QueryClient();
   
  @@ -29,12 +30,14 @@ interface DeploymentFeatures {
     mpcPublic: boolean;
     mpcPro: boolean;
     fabric: boolean;
  +  chatHistory: boolean;
   }
   
   const DEFAULT_FEATURES: DeploymentFeatures = {
     mpcPublic: true,
     mpcPro: false,
     fabric: false,
  +  chatHistory: false,
   };
   
   function App() {
  @@ -81,6 +84,7 @@ function App() {
             mpcPublic: data.features.mpcPublic !== false,
             mpcPro: !!data.features.mpcPro,
             fabric: !!data.features.fabric,
  +          chatHistory: !!data.features.chatHistory,
           };
           setFeatures(next);
           // If Pro is locked out by this deployment but the user had it
  @@ -152,6 +156,20 @@ function App() {
       setAppState(prev => ({ ...prev, selectedDataset: dataset, chatMode: true }));
     };
   
  +  const handleRestoreChatContext = (context: ChatHistoryContext) => {
  +    if (context.selectedModel) handleModelChange(context.selectedModel);
  +    if (context.reasoningEffort) handleReasoningEffortChange(context.reasoningEffort);
  +    if (context.stacMode) handleStacModeChange(context.stacMode);
  +    setAppState(prev => ({
  +      ...prev,
  +      selectedDataset: context.selectedDataset
  +        ? { ...context.selectedDataset, description: context.selectedDataset.description || '' }
  +        : null,
  +      chatMode: true,
  +      initialQuery: undefined,
  +    }));
  +  };
  +
     return (
       <QueryClientProvider client={queryClient}>
         <GlobalStyles />
  @@ -189,6 +207,8 @@ function App() {
                 onGeointToggle={setGeointMode}
                 selectedModel={selectedModel}
                 reasoningEffort={reasoningEffort}
  +              chatHistoryEnabled={features.chatHistory}
  +              onRestoreChatContext={handleRestoreChatContext}
                 stacMode={stacMode}
                 onStacModeChange={handleStacModeChange}
               />
  diff --git a/planetary-explorer/web-ui/src/components/Chat.tsx b/planetary-explorer/web-ui/src/components/Chat.tsx
  index 4505a2e..fa5cdc2 100644
  --- a/planetary-explorer/web-ui/src/components/Chat.tsx
  +++ b/planetary-explorer/web-ui/src/components/Chat.tsx
  @@ -2,13 +2,21 @@
   // Licensed under the MIT license.
   
   import React, { useState, useRef, useEffect } from 'react';
  -import { useMutation, useQuery } from '@tanstack/react-query';
  -import { apiService, Dataset, ChatMessage, MapContext } from '../services/api';
  +import { useMutation } from '@tanstack/react-query';
  +import { History } from 'lucide-react';
  +import { apiService, ChatHistoryContext, ChatHistorySnapshot, Dataset, ChatMessage, ChatHistorySession, MapContext } from '../services/api';
   import { enhanceMessageForMapVisualization, hasVisualizableData } from './PlanetaryExplorerMapIntegration';
   import vedaSearchService from '../services/vedaSearchService';
   import SourceChips from './SourceChips';
   import { TraceDrawer, ConfirmationCard, useToolTrace } from './trace';
   import type { PendingConfirm } from './trace';
  +import ChatHistoryDrawer from './ChatHistoryDrawer';
  +import {
  +  chatHistoryFingerprint,
  +  createChatHistoryMutationId,
  +  persistedChatMessages,
  +  restoreChatMessages,
  +} from '../utils/chatHistory';
   
   // Enhanced function to extract text from complex response objects
   function extractTextFromResponse(content: any): string {
  @@ -211,6 +219,9 @@ interface ChatProps {
     onComparisonResult?: (result: any) => void; // Callback for comparison analysis results (before/after data)
     selectedModel?: string; // Selected AI model (gpt-5)
     reasoningEffort?: string; // Selected GPT-5.6 reasoning effort
  +  chatHistoryEnabled?: boolean; // Durable Cosmos-backed chat archive
  +  onRestoreContext?: (context: ChatHistoryContext) => void;
  +  mapAnalysisPending?: boolean;
     stacMode?: 'public' | 'pro'; // Public MPC vs MPC Pro (private GeoCatalog) for STAC routing
   }
   
  @@ -237,6 +248,9 @@ const Chat: React.FC<ChatProps> = ({
     onComparisonResult,
     selectedModel,
     reasoningEffort,
  +  chatHistoryEnabled = false,
  +  onRestoreContext,
  +  mapAnalysisPending = false,
     stacMode,
   }) => {
     const [messages, setMessages] = useState<ChatMessage[]>([]);
  @@ -326,7 +340,23 @@ const Chat: React.FC<ChatProps> = ({
     const initialQueryRef = useRef(false);
     const [lastResponse, setLastResponse] = useState<string>('');
     // Add conversation ID to maintain context across messages
  -  const [conversationId] = useState(() => `web-session-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`);
  +  const [conversationId, setConversationId] = useState(() => `web-session-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`);
  +  const [historyOpen, setHistoryOpen] = useState(false);
  +  const [historySaveState, setHistorySaveState] = useState<'idle' | 'saving' | 'saved' | 'error'>('idle');
  +  const lastSavedSnapshotRef = useRef('');
  +  const lastServerRevisionRef = useRef(0);
  +  const historyGenerationRef = useRef(0);
  +  const historySaveInFlightRef = useRef(false);
  +  const historyRetryTimerRef = useRef<number | null>(null);
  +  const pendingHistorySaveRef = useRef<{
  +    sessionId: string;
  +    snapshot: Omit<ChatHistorySnapshot, 'expectedRevision'>;
  +    fingerprint: string;
  +    generation: number;
  +    attempts: number;
  +  } | null>(null);
  +  const retryHistorySaveRef = useRef<typeof pendingHistorySaveRef.current>(null);
  +  const restoringDatasetIdRef = useRef<string | null>(null);
     
     //  Local vision session ID tracking (for first message -> follow-ups)
     const [localVisionSessionId, setLocalVisionSessionId] = useState<string | null>(null);
  @@ -368,6 +398,13 @@ const Chat: React.FC<ChatProps> = ({
   
     // Add injection message when dataset is selected
     useEffect(() => {
  +    if (
  +      restoringDatasetIdRef.current &&
  +      selectedDataset?.id === restoringDatasetIdRef.current
  +    ) {
  +      restoringDatasetIdRef.current = null;
  +      return;
  +    }
       if (selectedDataset && chatMode) {
         const injectionMessage: ChatMessage = {
           role: 'assistant',
  @@ -2482,6 +2519,178 @@ const Chat: React.FC<ChatProps> = ({
       messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
     }, [messages]);
   
  +  const drainHistorySaves = async () => {
  +    if (historySaveInFlightRef.current) return;
  +    historySaveInFlightRef.current = true;
  +    let retryScheduled = false;
  +    try {
  +      while (retryHistorySaveRef.current || pendingHistorySaveRef.current) {
  +        const pending = retryHistorySaveRef.current || pendingHistorySaveRef.current!;
  +        if (retryHistorySaveRef.current) retryHistorySaveRef.current = null;
  +        else pendingHistorySaveRef.current = null;
  +        if (pending.generation !== historyGenerationRef.current) continue;
  +
  +        try {
  +          const saved = await apiService.saveChatSession(pending.sessionId, {
  +            ...pending.snapshot,
  +            expectedRevision: lastServerRevisionRef.current,
  +          });
  +          if (pending.generation !== historyGenerationRef.current) continue;
  +          lastServerRevisionRef.current = saved.revision;
  +          lastSavedSnapshotRef.current = pending.fingerprint;
  +          setHistorySaveState(pendingHistorySaveRef.current ? 'saving' : 'saved');
  +        } catch (error) {
  +          if (pending.generation === historyGenerationRef.current) {
  +            console.error(' Chat: Failed to save chat history:', error);
  +            const status = (error as any)?.response?.status;
  +            const attempts = pending.attempts + 1;
  +            if (status !== 409 && attempts <= 3) {
  +              retryHistorySaveRef.current = { ...pending, attempts };
  +              setHistorySaveState('saving');
  +              retryScheduled = true;
  +              historyRetryTimerRef.current = window.setTimeout(() => {
  +                historyRetryTimerRef.current = null;
  +                void drainHistorySaves();
  +              }, 1000 * attempts);
  +            } else {
  +              pendingHistorySaveRef.current = null;
  +              setHistorySaveState('error');
  +            }
  +          }
  +          break;
  +        }
  +      }
  +    } finally {
  +      historySaveInFlightRef.current = false;
  +      if ((retryHistorySaveRef.current || pendingHistorySaveRef.current) && !retryScheduled) {
  +        void drainHistorySaves();
  +      }
  +    }
  +  };
  +
  +  useEffect(() => () => {
  +    if (historyRetryTimerRef.current !== null) {
  +      window.clearTimeout(historyRetryTimerRef.current);
  +    }
  +  }, []);
  +
  +  const mapAnalysisBusy = mapAnalysisPending || (
  +    mobilityAnalysisResult?.type === 'thinking' ||
  +    mobilityAnalysisResult?.type === 'pending'
  +  );
  +
  +  useEffect(() => {
  +    if (
  +      !chatHistoryEnabled ||
  +      chatMutation.isPending ||
  +      privateSearchMutation.isPending ||
  +      partsPending ||
  +      mapAnalysisBusy
  +    ) {
  +      return;
  +    }
  +
  +    const persistedMessages = persistedChatMessages(messages);
  +    if (!persistedMessages.some((message) => message.role === 'user')) {
  +      return;
  +    }
  +    const snapshot: Omit<ChatHistorySnapshot, 'expectedRevision' | 'mutationId'> = {
  +      messages: persistedMessages,
  +      context: {
  +        selectedModel: selectedModel || undefined,
  +        reasoningEffort: reasoningEffort || undefined,
  +        selectedModule: selectedModule || undefined,
  +        selectedDataset: selectedDataset
  +          ? {
  +              id: selectedDataset.id,
  +              title: selectedDataset.title,
  +              description: selectedDataset.description,
  +            }
  +          : undefined,
  +        stacMode,
  +        pin: currentPin || undefined,
  +        map: mapContext
  +          ? {
  +              bounds: mapContext.bounds,
  +              current_collection: mapContext.current_collection,
  +              has_satellite_data: mapContext.has_satellite_data,
  +              imagery_url: mapContext.imagery_url,
  +              item_id: mapContext.item_id,
  +              datetime: mapContext.datetime,
  +              zoom_level: mapContext.zoom_level,
  +              tile_urls: mapContext.tile_urls?.slice(0, 20),
  +              vision_mode: mapContext.vision_mode,
  +              vision_pin: mapContext.vision_pin,
  +            }
  +          : undefined,
  +      },
  +    };
  +    const fingerprint = chatHistoryFingerprint(snapshot);
  +    if (fingerprint === lastSavedSnapshotRef.current) return;
  +
  +    setHistorySaveState('saving');
  +    pendingHistorySaveRef.current = {
  +      sessionId: conversationId,
  +      snapshot: {
  +        ...snapshot,
  +        mutationId: createChatHistoryMutationId(),
  +      },
  +      fingerprint,
  +      generation: historyGenerationRef.current,
  +      attempts: 0,
  +    };
  +    void drainHistorySaves();
  +  }, [
  +    chatHistoryEnabled,
  +    chatMutation.isPending,
  +    conversationId,
  +    currentPin,
  +    mapContext,
  +    mapAnalysisBusy,
  +    messages,
  +    partsPending,
  +    privateSearchMutation.isPending,
  +    reasoningEffort,
  +    selectedDataset,
  +    selectedModel,
  +    selectedModule,
  +    stacMode,
  +  ]);
  +
  +  const handleLoadHistorySession = (session: ChatHistorySession) => {
  +    if (chatMutation.isPending || privateSearchMutation.isPending || partsPending || mapAnalysisBusy || historySaveState === 'saving') {
  +      return;
  +    }
  +    const restoredMessages = restoreChatMessages(session.messages);
  +    historyGenerationRef.current += 1;
  +    pendingHistorySaveRef.current = null;
  +    retryHistorySaveRef.current = null;
  +    if (historyRetryTimerRef.current !== null) {
  +      window.clearTimeout(historyRetryTimerRef.current);
  +      historyRetryTimerRef.current = null;
  +    }
  +    lastServerRevisionRef.current = session.revision;
  +    restoringDatasetIdRef.current = session.context.selectedDataset?.id || null;
  +    onRestoreContext?.(session.context);
  +    lastSavedSnapshotRef.current = chatHistoryFingerprint({
  +      messages: restoredMessages,
  +      context: session.context,
  +    });
  +    setConversationId(session.sessionId);
  +    setMessages(restoredMessages);
  +    setPendingConfirms([]);
  +    setFeedback({});
  +    setHistorySaveState('saved');
  +  };
  +
  +  const historyBusy = (
  +    chatMutation.isPending ||
  +    privateSearchMutation.isPending ||
  +    partsPending ||
  +    mapAnalysisBusy ||
  +    historySaveState === 'saving'
  +  );
  +
     const getPlanetaryComputerExamples = (dataset: Dataset | null): string[] => {
       const defaultExamples = [
         'Show Sentinel-2 images of Seattle from last month',
  @@ -2580,9 +2789,39 @@ const Chat: React.FC<ChatProps> = ({
       <div className="right">
         <div className="chat chat-container">
           <div className="header">
  -          <span>Planetary Explorer Agent</span>
  +          <span className="chat-header-title">Planetary Explorer Agent</span>
  +          {chatHistoryEnabled && (
  +            <div className="chat-history-toolbar">
  +              <span className={`chat-save-state ${historySaveState}`} aria-live="polite">
  +                {historySaveState === 'saving' && 'Saving'}
  +                {historySaveState === 'saved' && 'Saved'}
  +                {historySaveState === 'error' && 'Save failed'}
  +              </span>
  +              <button
  +                type="button"
  +                className="chat-history-trigger"
  +                onClick={() => setHistoryOpen(true)}
  +                disabled={historyBusy}
  +                aria-label="Open saved chat sessions"
  +                title="Saved chat sessions"
  +              >
  +                <History size={17} aria-hidden="true" />
  +                <span>History</span>
  +              </button>
  +            </div>
  +          )}
           </div>
   
  +        <ChatHistoryDrawer
  +          activeSessionId={conversationId}
  +          enabled={chatHistoryEnabled}
  +          busy={historyBusy}
  +          open={historyOpen}
  +          onClose={() => setHistoryOpen(false)}
  +          onLoad={handleLoadHistorySession}
  +          onActiveSessionDeleted={onRestartSession}
  +        />
  +
           <div className="messages">
             {/* Examples removed to prevent flash on page load */}
   
  diff --git a/planetary-explorer/web-ui/src/components/ChatHistoryDrawer.tsx b/planetary-explorer/web-ui/src/components/ChatHistoryDrawer.tsx
  new file mode 100644
  index 0000000..4ca7d17
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/components/ChatHistoryDrawer.tsx
  @@ -0,0 +1,327 @@
  +// Copyright (c) Microsoft Corporation.
  +// Licensed under the MIT license.
  +
  +import React, { useRef, useState } from 'react';
  +import { useQuery } from '@tanstack/react-query';
  +import {
  +  Download,
  +  FileDown,
  +  FileUp,
  +  FolderOpen,
  +  RefreshCw,
  +  Trash2,
  +  X,
  +} from 'lucide-react';
  +
  +import {
  +  apiService,
  +  ChatHistoryAttachment,
  +  ChatHistorySession,
  +  ChatHistorySummary,
  +} from '../services/api';
  +
  +interface ChatHistoryDrawerProps {
  +  activeSessionId: string;
  +  busy: boolean;
  +  enabled: boolean;
  +  open: boolean;
  +  onClose: () => void;
  +  onLoad: (session: ChatHistorySession) => void;
  +  onActiveSessionDeleted?: () => void;
  +}
  +
  +function downloadBlob(blob: Blob, filename: string): void {
  +  const url = URL.createObjectURL(blob);
  +  const anchor = document.createElement('a');
  +  anchor.href = url;
  +  anchor.download = filename;
  +  document.body.appendChild(anchor);
  +  anchor.click();
  +  anchor.remove();
  +  window.setTimeout(() => URL.revokeObjectURL(url), 0);
  +}
  +
  +function formatSavedAt(value: string): string {
  +  const date = new Date(value);
  +  if (Number.isNaN(date.getTime())) return 'Unknown time';
  +  return new Intl.DateTimeFormat(undefined, {
  +    month: 'short',
  +    day: 'numeric',
  +    hour: 'numeric',
  +    minute: '2-digit',
  +  }).format(date);
  +}
  +
  +function formatBytes(value: number): string {
  +  if (!Number.isFinite(value) || value <= 0) return '0 B';
  +  const units = ['B', 'KB', 'MB', 'GB'];
  +  const unitIndex = Math.min(Math.floor(Math.log(value) / Math.log(1024)), units.length - 1);
  +  const amount = value / Math.pow(1024, unitIndex);
  +  return `${amount >= 10 || unitIndex === 0 ? amount.toFixed(0) : amount.toFixed(1)} ${units[unitIndex]}`;
  +}
  +
  +function formatActionError(error: any): string {
  +  const detail = error?.response?.data?.detail;
  +  if (typeof detail === 'string') return detail;
  +  if (Array.isArray(detail)) {
  +    const messages = detail
  +      .map((item) => typeof item?.msg === 'string' ? item.msg : null)
  +      .filter(Boolean);
  +    if (messages.length > 0) return messages.join(' ');
  +  }
  +  return error?.message || 'History action failed.';
  +}
  +
  +const ChatHistoryDrawer: React.FC<ChatHistoryDrawerProps> = ({
  +  activeSessionId,
  +  busy,
  +  enabled,
  +  open,
  +  onClose,
  +  onLoad,
  +  onActiveSessionDeleted,
  +}) => {
  +  const fileInputRef = useRef<HTMLInputElement>(null);
  +  const [actionKey, setActionKey] = useState<string | null>(null);
  +  const [actionError, setActionError] = useState<string | null>(null);
  +
  +  const historyQuery = useQuery({
  +    queryKey: ['chat-history-sessions'],
  +    queryFn: () => apiService.listChatSessions(),
  +    enabled: enabled && open,
  +    staleTime: 0,
  +  });
  +  const sessions = historyQuery.data || [];
  +  const activeSession = sessions.find((session) => session.sessionId === activeSessionId);
  +
  +  const runAction = async (key: string, action: () => Promise<void>) => {
  +    if (busy || actionKey !== null) return;
  +    setActionKey(key);
  +    setActionError(null);
  +    try {
  +      await action();
  +    } catch (error: any) {
  +      setActionError(formatActionError(error));
  +    } finally {
  +      setActionKey(null);
  +    }
  +  };
  +
  +  const openSession = (sessionId: string) => runAction(`open:${sessionId}`, async () => {
  +    const session = await apiService.getChatSession(sessionId);
  +    onLoad(session);
  +    onClose();
  +  });
  +
  +  const exportSession = (session: ChatHistorySummary) => runAction(`export:${session.sessionId}`, async () => {
  +    const blob = await apiService.exportChatSession(session.sessionId);
  +    downloadBlob(blob, `planetary-explorer-${session.sessionId}.zip`);
  +  });
  +
  +  const deleteSession = (session: ChatHistorySummary) => runAction(`delete:${session.sessionId}`, async () => {
  +    if (!window.confirm(`Delete ΓÇ£${session.title}ΓÇ¥ and its saved files?`)) return;
  +    await apiService.deleteChatSession(session.sessionId);
  +    await historyQuery.refetch();
  +    if (session.sessionId === activeSessionId) {
  +      onActiveSessionDeleted?.();
  +      onClose();
  +    }
  +  });
  +
  +  const uploadFile = (file: File) => runAction('upload', async () => {
  +    await apiService.uploadChatFile(activeSessionId, file);
  +    await historyQuery.refetch();
  +  });
  +
  +  const downloadAttachment = (
  +    session: ChatHistorySummary,
  +    attachment: ChatHistoryAttachment,
  +  ) => runAction(`file:${attachment.id}`, async () => {
  +    const blob = await apiService.downloadChatFile(session.sessionId, attachment.id);
  +    downloadBlob(blob, attachment.name);
  +  });
  +
  +  const deleteAttachment = (
  +    session: ChatHistorySummary,
  +    attachment: ChatHistoryAttachment,
  +  ) => runAction(`delete-file:${attachment.id}`, async () => {
  +    if (!window.confirm(`Delete ΓÇ£${attachment.name}ΓÇ¥?`)) return;
  +    await apiService.deleteChatFile(session.sessionId, attachment.id);
  +    await historyQuery.refetch();
  +  });
  +
  +  if (!open) return null;
  +
  +  return (
  +    <>
  +      <button
  +        className="chat-history-backdrop"
  +        type="button"
  +        aria-label="Close saved sessions"
  +        onClick={onClose}
  +      />
  +      <aside className="chat-history-drawer" aria-label="Saved chat sessions">
  +        <div className="chat-history-drawer-header">
  +          <div>
  +            <span className="chat-history-eyebrow">Workspace archive</span>
  +            <h2>Saved sessions</h2>
  +          </div>
  +          <div className="chat-history-header-actions">
  +            <button
  +              type="button"
  +              className="chat-history-icon-button"
  +              title="Refresh saved sessions"
  +              aria-label="Refresh saved sessions"
  +              onClick={() => historyQuery.refetch()}
  +              disabled={busy || historyQuery.isFetching}
  +            >
  +              <RefreshCw size={17} aria-hidden="true" />
  +            </button>
  +            <button
  +              type="button"
  +              className="chat-history-icon-button"
  +              title="Close"
  +              aria-label="Close saved sessions"
  +              onClick={onClose}
  +            >
  +              <X size={18} aria-hidden="true" />
  +            </button>
  +          </div>
  +        </div>
  +
  +        {actionError && <div className="chat-history-error" role="alert">{actionError}</div>}
  +
  +        {activeSession && (
  +          <section className="chat-history-current" aria-label="Current saved session">
  +            <div className="chat-history-section-heading">
  +              <span>Current session</span>
  +              <span>{activeSession.messageCount} messages</span>
  +            </div>
  +            <strong>{activeSession.title}</strong>
  +            <div className="chat-history-current-actions">
  +              <button
  +                type="button"
  +                onClick={() => fileInputRef.current?.click()}
  +                disabled={busy || actionKey !== null}
  +              >
  +                <FileUp size={16} aria-hidden="true" />
  +                Add data file
  +              </button>
  +              <button
  +                type="button"
  +                onClick={() => exportSession(activeSession)}
  +                disabled={busy || actionKey !== null}
  +              >
  +                <FileDown size={16} aria-hidden="true" />
  +                Test bundle
  +              </button>
  +              <input
  +                ref={fileInputRef}
  +                type="file"
  +                hidden
  +                onChange={(event) => {
  +                  const file = event.target.files?.[0];
  +                  if (file) uploadFile(file);
  +                  event.target.value = '';
  +                }}
  +              />
  +            </div>
  +
  +            {activeSession.attachments.length > 0 && (
  +              <div className="chat-history-files">
  +                {activeSession.attachments.map((attachment) => (
  +                  <div className="chat-history-file" key={attachment.id}>
  +                    <div>
  +                      <span>{attachment.name}</span>
  +                      <small>{formatBytes(attachment.size)}</small>
  +                    </div>
  +                    <div>
  +                      <button
  +                        type="button"
  +                        title={`Download ${attachment.name}`}
  +                        aria-label={`Download ${attachment.name}`}
  +                        onClick={() => downloadAttachment(activeSession, attachment)}
  +                        disabled={busy || actionKey !== null}
  +                      >
  +                        <Download size={15} aria-hidden="true" />
  +                      </button>
  +                      <button
  +                        type="button"
  +                        title={`Delete ${attachment.name}`}
  +                        aria-label={`Delete ${attachment.name}`}
  +                        onClick={() => deleteAttachment(activeSession, attachment)}
  +                        disabled={busy || actionKey !== null}
  +                      >
  +                        <Trash2 size={15} aria-hidden="true" />
  +                      </button>
  +                    </div>
  +                  </div>
  +                ))}
  +              </div>
  +            )}
  +          </section>
  +        )}
  +
  +        <div className="chat-history-list" aria-live="polite">
  +          {historyQuery.isLoading && <div className="chat-history-empty">Loading saved sessionsΓÇª</div>}
  +          {historyQuery.isError && (
  +            <div className="chat-history-error" role="alert">Saved sessions could not be loaded.</div>
  +          )}
  +          {!historyQuery.isLoading && !historyQuery.isError && sessions.length === 0 && (
  +            <div className="chat-history-empty">No saved sessions yet.</div>
  +          )}
  +          {sessions.map((session) => (
  +            <article
  +              className={`chat-history-session${session.sessionId === activeSessionId ? ' active' : ''}`}
  +              key={session.sessionId}
  +            >
  +              <button
  +                type="button"
  +                className="chat-history-session-main"
  +                onClick={() => openSession(session.sessionId)}
  +                disabled={busy || actionKey !== null}
  +              >
  +                <span className="chat-history-session-title">{session.title}</span>
  +                <span className="chat-history-session-meta">
  +                  {formatSavedAt(session.updatedAt)} ┬╖ {session.messageCount} messages
  +                  {session.attachments.length > 0 ? ` ┬╖ ${session.attachments.length} files` : ''}
  +                </span>
  +              </button>
  +              <div className="chat-history-session-actions">
  +                <button
  +                  type="button"
  +                  title="Open session"
  +                  aria-label={`Open ${session.title}`}
  +                  onClick={() => openSession(session.sessionId)}
  +                  disabled={busy || actionKey !== null}
  +                >
  +                  <FolderOpen size={16} aria-hidden="true" />
  +                </button>
  +                <button
  +                  type="button"
  +                  title="Download test bundle"
  +                  aria-label={`Download ${session.title} test bundle`}
  +                  onClick={() => exportSession(session)}
  +                  disabled={busy || actionKey !== null}
  +                >
  +                  <Download size={16} aria-hidden="true" />
  +                </button>
  +                <button
  +                  type="button"
  +                  title="Delete session"
  +                  aria-label={`Delete ${session.title}`}
  +                  onClick={() => deleteSession(session)}
  +                  disabled={busy || actionKey !== null}
  +                >
  +                  <Trash2 size={16} aria-hidden="true" />
  +                </button>
  +              </div>
  +            </article>
  +          ))}
  +        </div>
  +      </aside>
  +    </>
  +  );
  +};
  +
  +export default ChatHistoryDrawer;
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/components/MainApp.tsx b/planetary-explorer/web-ui/src/components/MainApp.tsx
  index c33c900..26aa087 100644
  --- a/planetary-explorer/web-ui/src/components/MainApp.tsx
  +++ b/planetary-explorer/web-ui/src/components/MainApp.tsx
  @@ -3,12 +3,13 @@
   
   import React, { useState, useEffect, useCallback } from 'react';
   import { useQuery } from '@tanstack/react-query';
  -import { apiService, Dataset } from '../services/api';
  +import { apiService, ChatHistoryContext, Dataset } from '../services/api';
   import Sidebar from './Sidebar';
   import Chat from './Chat';
   import MapView from './MapView';
   import ResizablePanel from './ResizablePanel';
   import { AppState } from '../App';
  +import { ChatHistoryMapRestore, restorableHistoryModule } from '../utils/mapHistory';
   
   interface MainAppProps {
     appState: AppState;
  @@ -19,11 +20,13 @@ interface MainAppProps {
     onGeointToggle: (enabled: boolean) => void; // Deprecated - will be removed
     selectedModel?: string;
     reasoningEffort?: string;
  +  chatHistoryEnabled?: boolean;
  +  onRestoreChatContext?: (context: ChatHistoryContext) => void;
     stacMode?: 'public' | 'pro';
     onStacModeChange?: (mode: 'public' | 'pro') => void;
   }
   
  -const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnToLanding, onRestartSession, geointMode, onGeointToggle, selectedModel, reasoningEffort, stacMode, onStacModeChange }) => {
  +const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnToLanding, onRestartSession, geointMode, onGeointToggle, selectedModel, reasoningEffort, chatHistoryEnabled, onRestoreChatContext, stacMode, onStacModeChange }) => {
     const [sidebarOpen, setSidebarOpen] = useState(false);
     const [lastChatResponse, setLastChatResponse] = useState<any>(null);
     const [chatPanelWidth, setChatPanelWidth] = useState(420);
  @@ -38,6 +41,8 @@ const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnTo
     const [awaitingComparisonQuery, setAwaitingComparisonQuery] = useState(false); // Flag to intercept next message
     const [terrainSession, setTerrainSession] = useState<{ sessionId: string | null; lat: number; lng: number } | null>(null); // Terrain session for multi-turn chat
     const [comparisonResult, setComparisonResult] = useState<any>(null); // Comparison analysis result (before/after data)
  +  const [historyMapRestore, setHistoryMapRestore] = useState<ChatHistoryMapRestore | null>(null);
  +  const [mapAnalysisInProgress, setMapAnalysisInProgress] = useState(false);
   
     // Handle comparison result from Chat component
     const handleComparisonResult = useCallback((result: any) => {
  @@ -127,6 +132,25 @@ const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnTo
       console.log('[DEL] MainApp: Clearing terrain session');
       setTerrainSession(null);
     }, []);
  +
  +  const handleRestoreChatContext = useCallback((context: ChatHistoryContext) => {
  +    const restoredModule = restorableHistoryModule(context.selectedModule);
  +    setSelectedModule(restoredModule);
  +    onGeointToggle(Boolean(restoredModule));
  +    setCurrentPin(context.pin || context.map?.vision_pin || null);
  +    setMapContext(context.map || null);
  +    setMobilityAnalysisResult(null);
  +    setMobilityPinCoords(null);
  +    setComparisonResult(null);
  +    setLastChatResponse(null);
  +    setTerrainSession(null);
  +    setSystemMessage(null);
  +    setHistoryMapRestore({
  +      token: Date.now(),
  +      context: { ...context, selectedModule: restoredModule || undefined },
  +    });
  +    onRestoreChatContext?.(context);
  +  }, [onGeointToggle, onRestoreChatContext]);
     
     // Clear ALL GEOINT sessions (called when starting a new STAC search)
     //
  @@ -410,6 +434,8 @@ const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnTo
             sidebarOpen={sidebarOpen}
             comparisonUserQuery={comparisonUserQuery}
             onTerrainSessionChange={handleTerrainSessionChange}
  +          historyRestore={historyMapRestore}
  +          onAnalysisStateChange={setMapAnalysisInProgress}
           />
         </div>
   
  @@ -442,6 +468,9 @@ const MainApp: React.FC<MainAppProps> = ({ appState, onDatasetSelect, onReturnTo
             onComparisonResult={handleComparisonResult}
             selectedModel={selectedModel}
             reasoningEffort={reasoningEffort}
  +          chatHistoryEnabled={chatHistoryEnabled}
  +          onRestoreContext={handleRestoreChatContext}
  +          mapAnalysisPending={mapAnalysisInProgress}
             stacMode={stacMode}
           />
         </ResizablePanel>
  diff --git a/planetary-explorer/web-ui/src/components/MapView.tsx b/planetary-explorer/web-ui/src/components/MapView.tsx
  index b290baf..b97cf5b 100644
  --- a/planetary-explorer/web-ui/src/components/MapView.tsx
  +++ b/planetary-explorer/web-ui/src/components/MapView.tsx
  @@ -5,6 +5,8 @@
   
   import React, { useEffect, useState, useRef } from 'react';
   import { Dataset, API_BASE_URL } from '../services/api';
  +import type { ChatHistoryMapRestore } from '../utils/mapHistory';
  +import { resolveLeafletTileSources, restoredLayerFromContext, restorableHistoryModule } from '../utils/mapHistory';
   import { authenticatedFetch } from '../services/authHelper';
   import { getGeoFmMapFeatures } from '../utils/geofmOverlay';
   // TileUrlGenerator removed - using backend-only tile URL generation (MPC best practice)
  @@ -79,6 +81,8 @@ interface MapViewProps {
     sidebarOpen?: boolean; // New: current state of sidebar
     comparisonUserQuery?: string | null; // New: user's comparison query to process
     onTerrainSessionChange?: (session: { sessionId: string | null; lat: number; lng: number } | null) => void; // Terrain session for multi-turn chat
  +  historyRestore?: ChatHistoryMapRestore | null;
  +  onAnalysisStateChange?: (inProgress: boolean) => void;
   }
   
   interface SatelliteData {
  @@ -129,7 +133,9 @@ const MapView: React.FC<MapViewProps> = ({
     onToggleSidebar,
     sidebarOpen = false,
     comparisonUserQuery = null,
  -  onTerrainSessionChange
  +  onTerrainSessionChange,
  +  historyRestore,
  +  onAnalysisStateChange,
   }) => {
     const mapRef = useRef<HTMLDivElement>(null);
     const [map, setMap] = useState<any>(null);
  @@ -245,6 +251,168 @@ const MapView: React.FC<MapViewProps> = ({
     // imagery, no text). Only the Azure Maps provider supports this; the
     // Leaflet fallback ignores it.
     const [showMapLabels, setShowMapLabels] = useState<boolean>(true);
  +  const restoredHistoryTokenRef = useRef<number | null>(null);
  +
  +  useEffect(() => {
  +    onAnalysisStateChange?.(analysisInProgress);
  +  }, [analysisInProgress, onAnalysisStateChange]);
  +
  +  useEffect(() => {
  +    if (
  +      !historyRestore ||
  +      !map ||
  +      !mapLoaded ||
  +      restoredHistoryTokenRef.current === historyRestore.token
  +    ) {
  +      return;
  +    }
  +
  +    const context = historyRestore.context;
  +    const restoredMap = context.map;
  +    const restoredModule = restorableHistoryModule(context.selectedModule);
  +    const restoredPin = context.pin || restoredMap?.vision_pin || null;
  +
  +    analysisAbortControllerRef.current?.abort();
  +    analysisAbortControllerRef.current = null;
  +    setAnalysisInProgress(false);
  +    setIsExpanding(false);
  +    setOriginalBounds(null);
  +
  +    const removeMarker = (marker: any) => {
  +      if (!marker) return;
  +      try {
  +        if (mapProvider === 'leaflet') map.removeLayer(marker);
  +        else map.markers?.remove(marker);
  +      } catch (error) {
  +        console.warn('MapView: could not remove stale module marker', error);
  +      }
  +    };
  +    removeMarker(mobilityPinARef.current?.marker);
  +    removeMarker(mobilityPinBRef.current?.marker);
  +    if (terrainAnalysisPin.marker !== pinState.marker) {
  +      removeMarker(terrainAnalysisPin.marker);
  +    }
  +    setMobilityPinA(null);
  +    setMobilityPinB(null);
  +    mobilityPinARef.current = null;
  +    mobilityPinBRef.current = null;
  +    setTerrainSessionId(null);
  +    onTerrainSessionChange?.(null);
  +    setComparisonState({
  +      awaitingUserQuery: false,
  +      beforeImagery: null,
  +      afterImagery: null,
  +      beforeScreenshot: null,
  +      afterScreenshot: null,
  +      showingBefore: true,
  +    });
  +
  +    const staleLayers = new Set<any>([
  +      ...(currentLayer ? [currentLayer] : []),
  +      ...activeTileLayersRef.current,
  +      ...leafletVectorLayersRef.current,
  +    ]);
  +    staleLayers.forEach((layer) => {
  +      try {
  +        if (mapProvider === 'leaflet' && map.hasLayer?.(layer)) map.removeLayer(layer);
  +        else if (mapProvider === 'azure') map.layers?.remove(layer);
  +      } catch (error) {
  +        console.warn('MapView: could not remove a stale history layer', error);
  +      }
  +    });
  +    setCurrentLayer(null);
  +    activeTileLayersRef.current = [];
  +    leafletVectorLayersRef.current = [];
  +    leafletRenderGenerationRef.current += 1;
  +    lastRenderedDataRef.current = null;
  +    isRenderingRef.current = false;
  +    setVisionScreenshot(null);
  +    geofmOverlayRef.current?.remove();
  +    geofmOverlayRef.current = null;
  +    resilienceMarkersRef.current.forEach((marker) => {
  +      try {
  +        if (mapProvider === 'leaflet') map.removeLayer(marker);
  +        else map.markers?.remove(marker);
  +      } catch (error) {
  +        console.warn('MapView: could not remove a stale facility marker', error);
  +      }
  +    });
  +    resilienceMarkersRef.current = [];
  +
  +    if (pinState.marker) {
  +      try {
  +        if (mapProvider === 'leaflet' && window.L) map.removeLayer(pinState.marker);
  +        else if (mapProvider === 'azure' && window.atlas) map.markers.remove(pinState.marker);
  +      } catch (error) {
  +        console.warn('MapView: could not remove the previous history pin', error);
  +      }
  +    }
  +
  +    let marker: any = null;
  +    if (restoredPin) {
  +      if (mapProvider === 'leaflet' && window.L) {
  +        const icon = window.L.divIcon({
  +          html: '<span style="display:block;width:18px;height:18px;border:3px solid white;border-radius:50%;background:#0d9488;box-shadow:0 2px 7px rgba(15,23,42,.35)"></span>',
  +          className: 'history-pin-marker',
  +          iconSize: [18, 18],
  +          iconAnchor: [9, 9],
  +        });
  +        marker = window.L.marker([restoredPin.lat, restoredPin.lng], { icon }).addTo(map);
  +      } else if (mapProvider === 'azure' && window.atlas) {
  +        marker = new window.atlas.HtmlMarker({
  +          position: [restoredPin.lng, restoredPin.lat],
  +          htmlContent: '<span style="display:block;width:18px;height:18px;border:3px solid white;border-radius:50%;background:#0d9488;box-shadow:0 2px 7px rgba(15,23,42,.35)"></span>',
  +          anchor: 'center',
  +        });
  +        map.markers.add(marker);
  +      }
  +    }
  +
  +    setSelectedModule(restoredModule);
  +    setPinMode(false);
  +    setFreePinMode(false);
  +    setVisionMode(restoredModule === 'vision');
  +    setTerrainAnalysisMode(restoredModule === 'terrain');
  +    setComparisonMode(false);
  +    setVisionPin(restoredPin || { lat: null, lng: null });
  +    setPinState(restoredPin
  +      ? { ...restoredPin, active: true, marker }
  +      : { lat: null, lng: null, active: false, marker: null });
  +    setTerrainAnalysisPin(restoredModule === 'terrain' && restoredPin
  +      ? { ...restoredPin, marker }
  +      : { lat: null, lng: null, marker: null });
  +    const restoredLayer = restoredLayerFromContext(restoredMap);
  +    setSatelliteData(restoredLayer);
  +    setOriginalBounds(restoredLayer?.bbox || null);
  +    setLastCollection(restoredMap?.current_collection || null);
  +
  +    const bounds = restoredMap?.bounds;
  +    if (bounds) {
  +      if (mapProvider === 'leaflet' && window.L) {
  +        map.fitBounds(
  +          [[bounds.south, bounds.west], [bounds.north, bounds.east]],
  +          { animate: false },
  +        );
  +      } else if (mapProvider === 'azure' && window.atlas) {
  +        map.setCamera({
  +          bounds: [bounds.west, bounds.south, bounds.east, bounds.north],
  +          padding: 32,
  +        });
  +      }
  +    } else if (restoredPin) {
  +      if (mapProvider === 'leaflet' && window.L) {
  +        map.setView([restoredPin.lat, restoredPin.lng], restoredMap?.zoom_level || 10, { animate: false });
  +      } else if (mapProvider === 'azure' && window.atlas) {
  +        map.setCamera({
  +          center: [restoredPin.lng, restoredPin.lat],
  +          zoom: restoredMap?.zoom_level || 10,
  +        });
  +      }
  +    }
  +
  +    restoredHistoryTokenRef.current = historyRestore.token;
  +    setMapPositionVersion((version) => version + 1);
  +  }, [historyRestore, map, mapLoaded, mapProvider]);
   
     // Initialize fallback map using OpenStreetMap when Azure Maps fails
     const initializeFallbackMap = () => {
  @@ -3282,14 +3450,21 @@ const MapView: React.FC<MapViewProps> = ({
     // Azure Maps clears all user-added layers when `styledata` fires (style reload).
     // This ref lets the styledata handler re-add the layers that were wiped.
     const activeTileLayersRef = useRef<any[]>([]);
  +  const leafletVectorLayersRef = useRef<any[]>([]);
  +  const leafletRenderGenerationRef = useRef(0);
     
     // Create a stable signature for satellite data to detect true changes
     const getSatelliteDataSignature = (data: SatelliteData | null): string | null => {
       if (!data) return null;
  -    // Use tile_url + item count + first item ID as a unique signature
  -    const firstItemId = data.items?.[0]?.id || 'none';
  -    const itemCount = data.items?.length || 0;
  -    return `${data.tile_url || 'no-url'}_${itemCount}_${firstItemId}`;
  +    return JSON.stringify({
  +      tileUrl: data.tile_url || null,
  +      bbox: data.bbox || null,
  +      itemIds: data.items?.map((item) => item.id) || [],
  +      tiles: data.all_tile_urls?.map((tile) => ({
  +        url: tile.tilejson_url,
  +        bbox: tile.bbox,
  +      })) || [],
  +    });
     };
     
     // Reset rendering flag when TRULY new satellite data arrives (different signature)
  @@ -4601,53 +4776,65 @@ const MapView: React.FC<MapViewProps> = ({
             return; // Exit and let the effect re-run with correct provider
           }
   
  -        // Remove existing satellite layer if any
  -        if (currentLayer) {
  -          console.log('??? MapView: Removing existing Leaflet layer');
  -          map.removeLayer(currentLayer);
  -          setCurrentLayer(null);
  -        }
  -
  -        // If we have a tile URL, add it as a tile layer
  -        if (satelliteData.tile_url && window.L) {
  -          console.log('??? MapView: Adding Leaflet tile layer:', satelliteData.tile_url);
  -
  -          // Configure tile layer options with bounds to prevent 404s outside data coverage
  -          const tileLayerOptions: any = {
  -            opacity: 0.8,
  -            attribution: 'Planetary Computer',
  -            errorTileUrl: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=', // Transparent 1x1 pixel
  -            maxNativeZoom: 18, // Prevent requests beyond available zoom levels
  -            tileSize: 256
  -          };
  -
  -          // Add bounds if available to limit tile requests to actual data coverage
  -          // This prevents excessive 404 errors for tiles outside the STAC item's bbox
  -          if (satelliteData.bbox && satelliteData.bbox.length === 4) {
  -            const [west, south, east, north] = satelliteData.bbox;
  -            tileLayerOptions.bounds = window.L.latLngBounds(
  -              window.L.latLng(south, west),
  -              window.L.latLng(north, east)
  -            );
  -            console.log('??? MapView: Tile layer constrained to bbox:', satelliteData.bbox);
  +        const renderGeneration = ++leafletRenderGenerationRef.current;
  +        const previousLayers = new Set<any>([
  +          ...(currentLayer ? [currentLayer] : []),
  +          ...activeTileLayersRef.current,
  +        ]);
  +        previousLayers.forEach((layer) => {
  +          try {
  +            if (map.hasLayer?.(layer)) map.removeLayer(layer);
  +          } catch (error) {
  +            console.warn('MapView: Failed to remove previous Leaflet layer:', error);
             }
  +        });
  +        activeTileLayersRef.current = [];
  +        setCurrentLayer(null);
   
  -          const tileLayer = window.L.tileLayer(satelliteData.tile_url, tileLayerOptions);
  -
  -          // Suppress tile load errors in console (404s outside bounds are expected)
  -          tileLayer.on('tileerror', (error: any) => {
  -            // Silently handle tile errors - they're expected for tiles outside data coverage
  -            // Only log if it's a repeated pattern that might indicate a real issue
  -            const url = error.tile?.src || 'unknown';
  -            if (Math.random() < 0.01) { // Log only 1% of errors to avoid console spam
  -              console.debug('? MapView: Tile not available (expected for tiles outside data bounds):', url.substring(0, 120));
  +        // Resolve raw TileJSON endpoints before passing templates to Leaflet.
  +        if (satelliteData.tile_url && window.L) {
  +          const collection = satelliteData.items?.[0]?.collection || 'unknown';
  +          void resolveLeafletTileSources(
  +            satelliteData,
  +            collection,
  +            fetchAndSignTileJSON,
  +          ).then((sources) => {
  +            if (renderGeneration !== leafletRenderGenerationRef.current) return;
  +            const layers = sources.map((source) => {
  +              const options: any = {
  +                opacity: 0.8,
  +                attribution: 'Planetary Computer',
  +                errorTileUrl: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=',
  +                maxNativeZoom: 18,
  +                tileSize: 256,
  +              };
  +              if (source.bbox?.length === 4) {
  +                const [west, south, east, north] = source.bbox;
  +                options.bounds = window.L.latLngBounds(
  +                  window.L.latLng(south, west),
  +                  window.L.latLng(north, east),
  +                );
  +              }
  +              const layer = window.L.tileLayer(source.tileTemplate, options);
  +              layer.on('tileerror', (error: any) => {
  +                if (Math.random() < 0.01) {
  +                  const url = error.tile?.src || 'unknown';
  +                  console.debug('MapView: Tile unavailable:', url.substring(0, 120));
  +                }
  +              });
  +              layer.addTo(map);
  +              return layer;
  +            });
  +            activeTileLayersRef.current = layers;
  +            setCurrentLayer(layers[0] || null);
  +            if (layers.length > 0) {
  +              lastRenderedDataRef.current = getSatelliteDataSignature(satelliteData);
  +            }
  +          }).catch((error) => {
  +            if (renderGeneration === leafletRenderGenerationRef.current) {
  +              console.error('MapView: Failed to resolve Leaflet TileJSON sources:', error);
               }
             });
  -
  -          tileLayer.addTo(map);
  -          setCurrentLayer(tileLayer);
  -
  -          console.log('? MapView: Successfully added Leaflet satellite tile layer');
           }
   
           // If we have map_data GeoJSON, add it as vector data
  @@ -4665,6 +4852,7 @@ const MapView: React.FC<MapViewProps> = ({
               });
   
               geoJsonLayer.addTo(map);
  +            leafletVectorLayersRef.current.push(geoJsonLayer);
             });
   
             console.log('? MapView: Successfully added GeoJSON featu to Leaflet');
  @@ -5831,9 +6019,10 @@ const MapView: React.FC<MapViewProps> = ({
         const currentCollection = satelliteData?.items?.[0]?.collection || lastCollection || null;
         
         // Build tile URLs array from satelliteData for Vision Agent
  -      const tileUrls = satelliteData?.all_tile_urls?.map((tile: { tilejson_url: string; item_id: string }) => ({
  +      const tileUrls = satelliteData?.all_tile_urls?.map((tile: { tilejson_url: string; item_id: string; bbox: number[] }) => ({
           tilejson_url: tile.tilejson_url,
           item_id: tile.item_id,
  +        bbox: tile.bbox,
           collection: currentCollection
         })) || [];
         
  @@ -5853,6 +6042,7 @@ const MapView: React.FC<MapViewProps> = ({
           bounds: bounds,
           imagery_base64: (visionMode || selectedModule === 'terrain' || selectedModule === 'mobility' || selectedModule === 'extreme_weather' || selectedModule === 'comparison' || selectedModule === 'building_damage') && visionScreenshot ? visionScreenshot : null, // Include screenshot for all GEOINT modules
           current_collection: currentCollection,
  +        imagery_url: satelliteData?.tile_url || satelliteData?.preview_url,
           tile_urls: tileUrls, // TiTiler URLs for Vision Agent raster analysis
           stac_items: stacItems, // Full STAC items with assets for NDVI computation
           item_id: satelliteData?.items?.[0]?.id || null,
  diff --git a/planetary-explorer/web-ui/src/components/__tests__/ChatHistoryDrawer.test.tsx b/planetary-explorer/web-ui/src/components/__tests__/ChatHistoryDrawer.test.tsx
  new file mode 100644
  index 0000000..4b7ec79
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/components/__tests__/ChatHistoryDrawer.test.tsx
  @@ -0,0 +1,121 @@
  +import React from 'react';
  +import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
  +import { fireEvent, render, screen, waitFor } from '@testing-library/react';
  +import { afterEach, describe, expect, it, vi } from 'vitest';
  +
  +import { apiService, ChatHistorySession } from '../../services/api';
  +import ChatHistoryDrawer from '../ChatHistoryDrawer';
  +
  +function renderDrawer(onLoad = vi.fn(), busy = false) {
  +  const queryClient = new QueryClient({
  +    defaultOptions: { queries: { retry: false } },
  +  });
  +  render(
  +    <QueryClientProvider client={queryClient}>
  +      <ChatHistoryDrawer
  +        activeSessionId="session-2"
  +        busy={busy}
  +        enabled
  +        open
  +        onClose={vi.fn()}
  +        onLoad={onLoad}
  +      />
  +    </QueryClientProvider>,
  +  );
  +  return onLoad;
  +}
  +
  +describe('ChatHistoryDrawer', () => {
  +  afterEach(() => {
  +    vi.restoreAllMocks();
  +  });
  +
  +  it('lists and restores a saved session', async () => {
  +    // Arrange
  +    const session: ChatHistorySession = {
  +      sessionId: 'session-1',
  +      schemaVersion: 1,
  +      revision: 1,
  +      title: 'Seattle flood scan',
  +      createdAt: '2026-08-26T12:00:00Z',
  +      updatedAt: '2026-08-26T12:01:00Z',
  +      messageCount: 2,
  +      attachments: [],
  +      messages: [
  +        { role: 'user', content: 'Show Seattle flooding', timestamp: new Date('2026-08-26T12:00:00Z') },
  +        { role: 'assistant', content: 'Loaded one scene', timestamp: new Date('2026-08-26T12:01:00Z') },
  +      ],
  +      context: { selectedModel: 'gpt-5' },
  +    };
  +    vi.spyOn(apiService, 'listChatSessions').mockResolvedValue([session]);
  +    vi.spyOn(apiService, 'getChatSession').mockResolvedValue(session);
  +    const onLoad = renderDrawer();
  +
  +    // Act
  +    await screen.findByText('Seattle flood scan');
  +    fireEvent.click(screen.getByRole('button', { name: 'Open Seattle flood scan' }));
  +
  +    // Assert
  +    await waitFor(() => expect(onLoad).toHaveBeenCalledWith(session));
  +  });
  +
  +  it('renders a structured upload error without crashing', async () => {
  +    // Arrange
  +    const currentSession: ChatHistorySession = {
  +      sessionId: 'session-2',
  +      schemaVersion: 1,
  +      revision: 1,
  +      title: 'Current test',
  +      createdAt: '2026-08-26T12:00:00Z',
  +      updatedAt: '2026-08-26T12:01:00Z',
  +      messageCount: 1,
  +      attachments: [],
  +      messages: [{ role: 'user', content: 'Test', timestamp: new Date() }],
  +      context: {},
  +    };
  +    vi.spyOn(apiService, 'listChatSessions').mockResolvedValue([currentSession]);
  +    vi.spyOn(apiService, 'uploadChatFile').mockRejectedValue({
  +      response: { data: { detail: [{ msg: 'Field required' }] } },
  +    });
  +    renderDrawer();
  +    expect(await screen.findByRole('region', { name: 'Current saved session' }))
  +      .toHaveTextContent('Current test');
  +
  +    // Act
  +    const input = document.querySelector('input[type="file"]') as HTMLInputElement;
  +    fireEvent.change(input, {
  +      target: { files: [new File(['x'], 'test.csv', { type: 'text/csv' })] },
  +    });
  +
  +    // Assert
  +    expect(await screen.findByRole('alert')).toHaveTextContent('Field required');
  +  });
  +
  +  it('blocks session switching while chat work is pending', async () => {
  +    // Arrange
  +    const session: ChatHistorySession = {
  +      sessionId: 'session-1',
  +      schemaVersion: 1,
  +      revision: 1,
  +      title: 'Pending test',
  +      createdAt: '2026-08-26T12:00:00Z',
  +      updatedAt: '2026-08-26T12:01:00Z',
  +      messageCount: 1,
  +      attachments: [],
  +      messages: [{ role: 'user', content: 'Test', timestamp: new Date() }],
  +      context: {},
  +    };
  +    vi.spyOn(apiService, 'listChatSessions').mockResolvedValue([session]);
  +    const getSession = vi.spyOn(apiService, 'getChatSession');
  +    renderDrawer(vi.fn(), true);
  +
  +    // Act
  +    await screen.findByText('Pending test');
  +    const openButton = screen.getByRole('button', { name: 'Open Pending test' });
  +
  +    // Assert
  +    expect(openButton).toBeDisabled();
  +    fireEvent.click(openButton);
  +    expect(getSession).not.toHaveBeenCalled();
  +  });
  +});
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/services/__tests__/api.chatHistory.test.ts b/planetary-explorer/web-ui/src/services/__tests__/api.chatHistory.test.ts
  new file mode 100644
  index 0000000..7b59781
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/services/__tests__/api.chatHistory.test.ts
  @@ -0,0 +1,68 @@
  +import { beforeEach, describe, expect, it, vi } from 'vitest';
  +
  +import { apiService } from '../api';
  +
  +describe('ApiService chat history', () => {
  +  const get = vi.fn();
  +  const put = vi.fn();
  +  const post = vi.fn();
  +
  +  beforeEach(() => {
  +    get.mockReset();
  +    put.mockReset();
  +    post.mockReset();
  +    (apiService as any).api = { get, post, put };
  +  });
  +
  +  it('saves a session snapshot to the encoded session route', async () => {
  +    // Arrange
  +    const snapshot = {
  +      expectedRevision: 0,
  +      mutationId: 'frontend-api-save',
  +      messages: [{ role: 'user' as const, content: 'Show Seattle', timestamp: new Date() }],
  +      context: { selectedModel: 'gpt-5' },
  +    };
  +    put.mockResolvedValue({ data: { sessionId: 'session/one', ...snapshot } });
  +
  +    // Act
  +    await apiService.saveChatSession('session/one', snapshot);
  +
  +    // Assert
  +    expect(put).toHaveBeenCalledWith(
  +      '/api/chat-history/sessions/session%2Fone',
  +      snapshot,
  +    );
  +  });
  +
  +  it('requests an exported session as a blob', async () => {
  +    // Arrange
  +    const archive = new Blob(['zip']);
  +    get.mockResolvedValue({ data: archive });
  +
  +    // Act
  +    const result = await apiService.exportChatSession('session-1');
  +
  +    // Assert
  +    expect(get).toHaveBeenCalledWith(
  +      '/api/chat-history/sessions/session-1/export',
  +      { responseType: 'blob' },
  +    );
  +    expect(result).toBe(archive);
  +  });
  +
  +  it('clears the JSON content type for multipart file uploads', async () => {
  +    // Arrange
  +    const file = new File(['lat,lng\n'], 'coordinates.csv', { type: 'text/csv' });
  +    post.mockResolvedValue({ data: { id: 'file-1', name: file.name } });
  +
  +    // Act
  +    await apiService.uploadChatFile('session-1', file);
  +
  +    // Assert
  +    expect(post).toHaveBeenCalledWith(
  +      '/api/chat-history/sessions/session-1/files',
  +      expect.any(FormData),
  +      { headers: { 'Content-Type': undefined } },
  +    );
  +  });
  +});
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/services/api.ts b/planetary-explorer/web-ui/src/services/api.ts
  index 6b90bbf..abe17ca 100644
  --- a/planetary-explorer/web-ui/src/services/api.ts
  +++ b/planetary-explorer/web-ui/src/services/api.ts
  @@ -90,6 +90,53 @@ export interface ChatMessage {
     }>;
   }
   
  +export interface ChatHistoryAttachment {
  +  id: string;
  +  name: string;
  +  contentType: string;
  +  size: number;
  +  sha256: string;
  +  createdAt: string;
  +}
  +
  +export interface ChatHistorySummary {
  +  sessionId: string;
  +  title: string;
  +  createdAt: string;
  +  updatedAt: string;
  +  messageCount: number;
  +  attachments: ChatHistoryAttachment[];
  +}
  +
  +export interface ChatHistorySession extends ChatHistorySummary {
  +  schemaVersion: number;
  +  revision: number;
  +  messages: ChatMessage[];
  +  context: ChatHistoryContext;
  +}
  +
  +export interface ChatHistoryContext {
  +  selectedModel?: string;
  +  reasoningEffort?: string;
  +  selectedModule?: string;
  +  selectedDataset?: {
  +    id: string;
  +    title: string;
  +    description?: string;
  +  };
  +  stacMode?: 'public' | 'pro';
  +  pin?: { lat: number; lng: number };
  +  map?: Partial<MapContext>;
  +}
  +
  +export interface ChatHistorySnapshot {
  +  title?: string;
  +  expectedRevision: number;
  +  mutationId: string;
  +  messages: ChatMessage[];
  +  context: ChatHistoryContext;
  +}
  +
   export interface MapContext {
     bounds?: {
       north: number;
  @@ -101,11 +148,15 @@ export interface MapContext {
     };
     imagery_base64?: string; // Base64 screenshot from MapView canvas
     imagery_url?: string;
  +  item_id?: string;
  +  datetime?: string;
  +  zoom_level?: number;
     current_collection?: string;
     tile_urls?: Array<{  // TiTiler URLs from prior STAC response for Vision Agent
       tilejson_url: string;
       item_id?: string;
       collection?: string;
  +    bbox?: number[];
     }>;
     // Full STAC items captured by MapView after a search. Includes
     // `assets` with `href` URLs needed by the backend `sample_raster_value`
  @@ -1127,6 +1178,79 @@ class ApiService {
       return res.data?.results || [];
     }
   
  +  async listChatSessions(): Promise<ChatHistorySummary[]> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const response = await this.api.get('/api/chat-history/sessions');
  +    return response.data?.sessions || [];
  +  }
  +
  +  async getChatSession(sessionId: string): Promise<ChatHistorySession> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const response = await this.api.get(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}`,
  +    );
  +    return response.data;
  +  }
  +
  +  async saveChatSession(
  +    sessionId: string,
  +    snapshot: ChatHistorySnapshot,
  +  ): Promise<ChatHistorySession> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const response = await this.api.put(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}`,
  +      snapshot,
  +    );
  +    return response.data;
  +  }
  +
  +  async deleteChatSession(sessionId: string): Promise<void> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    await this.api.delete(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}`,
  +    );
  +  }
  +
  +  async uploadChatFile(sessionId: string, file: File): Promise<ChatHistoryAttachment> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const form = new FormData();
  +    form.append('file', file);
  +    const response = await this.api.post(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}/files`,
  +      form,
  +      { headers: { 'Content-Type': undefined } },
  +    );
  +    return response.data;
  +  }
  +
  +  async downloadChatFile(
  +    sessionId: string,
  +    attachmentId: string,
  +  ): Promise<Blob> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const response = await this.api.get(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}/files/${encodeURIComponent(attachmentId)}`,
  +      { responseType: 'blob' },
  +    );
  +    return response.data;
  +  }
  +
  +  async deleteChatFile(sessionId: string, attachmentId: string): Promise<void> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    await this.api.delete(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}/files/${encodeURIComponent(attachmentId)}`,
  +    );
  +  }
  +
  +  async exportChatSession(sessionId: string): Promise<Blob> {
  +    if (!this.api) throw new Error('API service not initialized');
  +    const response = await this.api.get(
  +      `/api/chat-history/sessions/${encodeURIComponent(sessionId)}/export`,
  +      { responseType: 'blob' },
  +    );
  +    return response.data;
  +  }
  +
     // ------------------------------------------------------------------
     // Site Audit (data center siting dossier across Fabric + MPC + AI Search)
     // ------------------------------------------------------------------
  diff --git a/planetary-explorer/web-ui/src/styles/GlobalStyles.tsx b/planetary-explorer/web-ui/src/styles/GlobalStyles.tsx
  index 5f5ffaf..67a20a1 100644
  --- a/planetary-explorer/web-ui/src/styles/GlobalStyles.tsx
  +++ b/planetary-explorer/web-ui/src/styles/GlobalStyles.tsx
  @@ -922,6 +922,7 @@ export const GlobalStyles = () => (
         display: flex;
         flex-direction: column;
         height: 100%;
  +      position: relative;
         background: #ffffff;
         border-radius: 12px;
         box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
  @@ -939,6 +940,346 @@ export const GlobalStyles = () => (
         justify-content: center;
         border-bottom: none;
         box-shadow: 0 2px 8px rgba(59, 130, 246, 0.15);
  +      position: relative;
  +    }
  +
  +    .chat-header-title {
  +      max-width: calc(100% - 118px);
  +      min-width: 0;
  +      overflow: hidden;
  +      text-overflow: ellipsis;
  +      white-space: nowrap;
  +    }
  +
  +    .chat-history-toolbar {
  +      position: absolute;
  +      top: 50%;
  +      right: 14px;
  +      transform: translateY(-50%);
  +      display: flex;
  +      align-items: center;
  +      gap: 8px;
  +    }
  +
  +    .chat-save-state {
  +      min-width: 44px;
  +      color: rgba(255, 255, 255, 0.76);
  +      font-size: 11px;
  +      font-weight: 500;
  +      text-align: right;
  +    }
  +
  +    .chat-save-state.error {
  +      color: #fef2f2;
  +    }
  +
  +    .chat-history-trigger {
  +      height: 34px;
  +      padding: 0 10px;
  +      border: 1px solid rgba(255, 255, 255, 0.4);
  +      border-radius: 6px;
  +      background: rgba(15, 23, 42, 0.16);
  +      color: #fff;
  +      display: inline-flex;
  +      align-items: center;
  +      gap: 6px;
  +      font: inherit;
  +      font-size: 12px;
  +      cursor: pointer;
  +    }
  +
  +    .chat-history-trigger:hover {
  +      background: rgba(15, 23, 42, 0.28);
  +    }
  +
  +    .chat-history-backdrop {
  +      position: absolute;
  +      inset: 0;
  +      z-index: 40;
  +      border: 0;
  +      padding: 0;
  +      background: rgba(15, 23, 42, 0.34);
  +      cursor: default;
  +    }
  +
  +    .chat-history-drawer {
  +      position: absolute;
  +      inset: 0 0 0 auto;
  +      z-index: 41;
  +      width: min(390px, 100vw);
  +      background: #f8fafc;
  +      border-left: 1px solid #cbd5e1;
  +      box-shadow: -12px 0 32px rgba(15, 23, 42, 0.18);
  +      color: #1e293b;
  +      display: flex;
  +      flex-direction: column;
  +      overflow: hidden;
  +    }
  +
  +    .chat-history-drawer-header {
  +      min-height: 76px;
  +      padding: 14px 16px;
  +      border-bottom: 1px solid #dbe3ec;
  +      background: #fff;
  +      display: flex;
  +      align-items: center;
  +      justify-content: space-between;
  +      gap: 12px;
  +    }
  +
  +    .chat-history-drawer-header h2 {
  +      margin: 2px 0 0;
  +      font-size: 18px;
  +      line-height: 1.2;
  +      letter-spacing: 0;
  +    }
  +
  +    .chat-history-eyebrow {
  +      color: #0f766e;
  +      font-size: 10px;
  +      font-weight: 700;
  +      text-transform: uppercase;
  +      letter-spacing: 0;
  +    }
  +
  +    .chat-history-header-actions,
  +    .chat-history-session-actions,
  +    .chat-history-file > div:last-child {
  +      display: flex;
  +      align-items: center;
  +      gap: 4px;
  +    }
  +
  +    .chat-history-icon-button,
  +    .chat-history-session-actions button,
  +    .chat-history-file button {
  +      width: 32px;
  +      height: 32px;
  +      padding: 0;
  +      border: 1px solid transparent;
  +      border-radius: 6px;
  +      background: transparent;
  +      color: #475569;
  +      display: inline-flex;
  +      align-items: center;
  +      justify-content: center;
  +      cursor: pointer;
  +    }
  +
  +    .chat-history-icon-button:hover,
  +    .chat-history-session-actions button:hover,
  +    .chat-history-file button:hover {
  +      border-color: #cbd5e1;
  +      background: #fff;
  +      color: #0f766e;
  +    }
  +
  +    .chat-history-icon-button:disabled,
  +    .chat-history-session-actions button:disabled,
  +    .chat-history-file button:disabled,
  +    .chat-history-current-actions button:disabled {
  +      opacity: 0.48;
  +      cursor: wait;
  +    }
  +
  +    .chat-history-current {
  +      padding: 14px 16px;
  +      border-bottom: 1px solid #dbe3ec;
  +      background: #ecfdf5;
  +    }
  +
  +    .chat-history-section-heading {
  +      margin-bottom: 5px;
  +      color: #64748b;
  +      display: flex;
  +      justify-content: space-between;
  +      gap: 12px;
  +      font-size: 10px;
  +      font-weight: 700;
  +      text-transform: uppercase;
  +      letter-spacing: 0;
  +    }
  +
  +    .chat-history-current > strong {
  +      display: block;
  +      overflow: hidden;
  +      text-overflow: ellipsis;
  +      white-space: nowrap;
  +      font-size: 14px;
  +    }
  +
  +    .chat-history-current-actions {
  +      margin-top: 10px;
  +      display: grid;
  +      grid-template-columns: 1fr 1fr;
  +      gap: 8px;
  +    }
  +
  +    .chat-history-current-actions button {
  +      min-width: 0;
  +      height: 36px;
  +      padding: 0 10px;
  +      border: 1px solid #99f6e4;
  +      border-radius: 6px;
  +      background: #fff;
  +      color: #115e59;
  +      display: inline-flex;
  +      align-items: center;
  +      justify-content: center;
  +      gap: 6px;
  +      font: inherit;
  +      font-size: 12px;
  +      font-weight: 600;
  +      cursor: pointer;
  +    }
  +
  +    .chat-history-current-actions button:hover {
  +      border-color: #0d9488;
  +    }
  +
  +    .chat-history-files {
  +      margin-top: 10px;
  +      border-top: 1px solid #a7f3d0;
  +    }
  +
  +    .chat-history-file {
  +      min-height: 42px;
  +      border-bottom: 1px solid #d1fae5;
  +      display: flex;
  +      align-items: center;
  +      justify-content: space-between;
  +      gap: 8px;
  +    }
  +
  +    .chat-history-file > div:first-child {
  +      min-width: 0;
  +      display: flex;
  +      flex-direction: column;
  +    }
  +
  +    .chat-history-file span {
  +      overflow: hidden;
  +      text-overflow: ellipsis;
  +      white-space: nowrap;
  +      font-size: 12px;
  +      font-weight: 600;
  +    }
  +
  +    .chat-history-file small {
  +      color: #64748b;
  +      font-size: 10px;
  +    }
  +
  +    .chat-history-list {
  +      flex: 1;
  +      overflow-y: auto;
  +      background: #f8fafc;
  +    }
  +
  +    .chat-history-session {
  +      min-height: 76px;
  +      padding: 10px 10px 10px 13px;
  +      border-bottom: 1px solid #e2e8f0;
  +      border-left: 3px solid transparent;
  +      background: #fff;
  +      display: grid;
  +      grid-template-columns: minmax(0, 1fr) auto;
  +      align-items: center;
  +      gap: 6px;
  +    }
  +
  +    .chat-history-session.active {
  +      border-left-color: #0d9488;
  +      background: #f0fdfa;
  +    }
  +
  +    .chat-history-session-main {
  +      min-width: 0;
  +      padding: 4px;
  +      border: 0;
  +      background: transparent;
  +      color: inherit;
  +      display: flex;
  +      flex-direction: column;
  +      align-items: flex-start;
  +      text-align: left;
  +      cursor: pointer;
  +    }
  +
  +    .chat-history-session-title {
  +      width: 100%;
  +      overflow: hidden;
  +      text-overflow: ellipsis;
  +      white-space: nowrap;
  +      font-size: 13px;
  +      font-weight: 650;
  +    }
  +
  +    .chat-history-session-meta {
  +      width: 100%;
  +      margin-top: 4px;
  +      overflow: hidden;
  +      text-overflow: ellipsis;
  +      white-space: nowrap;
  +      color: #64748b;
  +      font-size: 10px;
  +    }
  +
  +    .chat-history-empty,
  +    .chat-history-error {
  +      margin: 16px;
  +      padding: 12px;
  +      border-radius: 6px;
  +      font-size: 12px;
  +      line-height: 1.4;
  +    }
  +
  +    .chat-history-empty {
  +      border: 1px dashed #cbd5e1;
  +      color: #64748b;
  +      text-align: center;
  +    }
  +
  +    .chat-history-error {
  +      border: 1px solid #fecaca;
  +      background: #fef2f2;
  +      color: #991b1b;
  +    }
  +
  +    @media (max-width: 560px) {
  +      .chat-header-title {
  +        max-width: 145px;
  +      }
  +
  +      .chat-save-state {
  +        display: none;
  +      }
  +
  +      .chat-history-toolbar {
  +        right: 10px;
  +      }
  +
  +      .chat-history-trigger span {
  +        display: none;
  +      }
  +
  +      .chat-history-trigger {
  +        width: 34px;
  +        padding: 0;
  +        justify-content: center;
  +      }
  +
  +      .chat-history-drawer {
  +        position: fixed;
  +        inset: 0;
  +        width: 100vw;
  +        max-width: 100vw;
  +        border-left: 0;
  +      }
  +
  +      .chat-history-backdrop {
  +        position: fixed;
  +      }
       }
   
       /* Remove earth icon from header */
  diff --git a/planetary-explorer/web-ui/src/utils/__tests__/chatHistory.test.ts b/planetary-explorer/web-ui/src/utils/__tests__/chatHistory.test.ts
  new file mode 100644
  index 0000000..f5ffd65
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/utils/__tests__/chatHistory.test.ts
  @@ -0,0 +1,45 @@
  +import { describe, expect, it } from 'vitest';
  +
  +import { ChatHistorySnapshot } from '../../services/api';
  +import {
  +  chatHistoryFingerprint,
  +  createChatHistoryMutationId,
  +  restoreChatMessages,
  +} from '../chatHistory';
  +
  +describe('chat history snapshots', () => {
  +  it('changes the autosave fingerprint when only context changes', () => {
  +    // Arrange
  +    const base: Omit<ChatHistorySnapshot, 'expectedRevision' | 'mutationId'> = {
  +      messages: [{ role: 'user', content: 'Show Seattle', timestamp: new Date(0) }],
  +      context: { selectedModel: 'gpt-5', stacMode: 'public' },
  +    };
  +    const changed: Omit<ChatHistorySnapshot, 'expectedRevision' | 'mutationId'> = {
  +      ...base,
  +      context: { ...base.context, stacMode: 'pro' },
  +    };
  +
  +    // Act & Assert
  +    expect(chatHistoryFingerprint(base)).not.toBe(chatHistoryFingerprint(changed));
  +  });
  +
  +  it('creates URL-safe mutation identifiers for idempotent saves', () => {
  +    expect(createChatHistoryMutationId()).toMatch(/^[A-Za-z0-9._:-]+$/);
  +  });
  +
  +  it('restores message timestamps as Date values and clears thinking state', () => {
  +    // Act
  +    const restored = restoreChatMessages([
  +      {
  +        role: 'assistant',
  +        content: 'Saved answer',
  +        timestamp: '2026-08-26T12:00:00Z' as unknown as Date,
  +        isThinking: true,
  +      },
  +    ]);
  +
  +    // Assert
  +    expect(restored[0].timestamp).toBeInstanceOf(Date);
  +    expect(restored[0].isThinking).toBe(false);
  +  });
  +});
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/utils/__tests__/mapHistory.test.ts b/planetary-explorer/web-ui/src/utils/__tests__/mapHistory.test.ts
  new file mode 100644
  index 0000000..b4f1217
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/utils/__tests__/mapHistory.test.ts
  @@ -0,0 +1,91 @@
  +import { describe, expect, it } from 'vitest';
  +
  +import {
  +  resolveLeafletTileSources,
  +  restoredLayerFromContext,
  +  restorableHistoryModule,
  +} from '../mapHistory';
  +
  +describe('map history restoration', () => {
  +  it('rejects disabled modules during restoration', () => {
  +    expect(restorableHistoryModule('comparison')).toBeNull();
  +    expect(restorableHistoryModule('mobility')).toBeNull();
  +    expect(restorableHistoryModule('forecast')).toBe('forecast');
  +    expect(restorableHistoryModule('timeseries')).toBe('timeseries');
  +    expect(restorableHistoryModule('terrain')).toBe('terrain');
  +  });
  +
  +  it('reconstructs a renderable layer from saved map context', () => {
  +    const layer = restoredLayerFromContext({
  +      bounds: {
  +        north: 48,
  +        south: 47,
  +        east: -121,
  +        west: -123,
  +        center_lat: 47.5,
  +        center_lng: -122,
  +      },
  +      current_collection: 'sentinel-2-l2a',
  +      imagery_url: 'https://tiles.example/{z}/{x}/{y}.png',
  +      item_id: 'item-1',
  +      datetime: '2026-08-26T00:00:00Z',
  +      tile_urls: [
  +        {
  +          tilejson_url: 'https://tiles.example/item-1/tilejson.json',
  +          item_id: 'item-1',
  +          bbox: [-123, 47, -122, 48],
  +        },
  +        {
  +          tilejson_url: 'https://tiles.example/item-2/tilejson.json',
  +          item_id: 'item-2',
  +          bbox: [-122, 47, -121, 48],
  +        },
  +      ],
  +    });
  +
  +    expect(layer).toMatchObject({
  +      bbox: [-123, 47, -121, 48],
  +      tile_url: 'https://tiles.example/{z}/{x}/{y}.png',
  +      items: [{ id: 'item-1', collection: 'sentinel-2-l2a' }],
  +      all_tile_urls: [
  +        {
  +          item_id: 'item-1',
  +          bbox: [-123, 47, -122, 48],
  +          tilejson_url: 'https://tiles.example/item-1/tilejson.json',
  +        },
  +        {
  +          item_id: 'item-2',
  +          bbox: [-122, 47, -121, 48],
  +          tilejson_url: 'https://tiles.example/item-2/tilejson.json',
  +        },
  +      ],
  +    });
  +  });
  +
  +  it('resolves multiple TileJSON endpoints into Leaflet templates with per-tile bounds', async () => {
  +    // Arrange
  +    const fetchTileJson = async (url: string) => ({
  +      success: true,
  +      originalUrl: url,
  +      tileTemplate: url.replace('tilejson.json', '{z}/{x}/{y}.png'),
  +    });
  +
  +    // Act
  +    const sources = await resolveLeafletTileSources(
  +      {
  +        all_tile_urls: [
  +          { tilejson_url: 'https://tiles.example/a/tilejson.json', bbox: [0, 1, 2, 3] },
  +          { tilejson_url: 'https://tiles.example/b/tilejson.json', bbox: [2, 1, 4, 3] },
  +        ],
  +      },
  +      'sentinel-2-l2a',
  +      fetchTileJson,
  +    );
  +
  +    // Assert
  +    expect(sources).toEqual([
  +      { tileTemplate: 'https://tiles.example/a/{z}/{x}/{y}.png', bbox: [0, 1, 2, 3] },
  +      { tileTemplate: 'https://tiles.example/b/{z}/{x}/{y}.png', bbox: [2, 1, 4, 3] },
  +    ]);
  +  });
  +});
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/utils/chatHistory.ts b/planetary-explorer/web-ui/src/utils/chatHistory.ts
  new file mode 100644
  index 0000000..7b7aafa
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/utils/chatHistory.ts
  @@ -0,0 +1,24 @@
  +import { ChatHistorySnapshot, ChatMessage } from '../services/api';
  +
  +export function persistedChatMessages(messages: ChatMessage[]): ChatMessage[] {
  +  return messages.filter((message) => !message.isThinking);
  +}
  +
  +export function chatHistoryFingerprint(
  +  snapshot: Omit<ChatHistorySnapshot, 'expectedRevision' | 'mutationId'>,
  +): string {
  +  return JSON.stringify(snapshot);
  +}
  +
  +export function createChatHistoryMutationId(): string {
  +  if (globalThis.crypto?.randomUUID) return globalThis.crypto.randomUUID();
  +  return `mutation-${Date.now()}-${Math.random().toString(36).slice(2, 12)}`;
  +}
  +
  +export function restoreChatMessages(messages: ChatMessage[]): ChatMessage[] {
  +  return messages.map((message) => ({
  +    ...message,
  +    timestamp: new Date(message.timestamp),
  +    isThinking: false,
  +  }));
  +}
  \ No newline at end of file
  diff --git a/planetary-explorer/web-ui/src/utils/mapHistory.ts b/planetary-explorer/web-ui/src/utils/mapHistory.ts
  new file mode 100644
  index 0000000..f10c88f
  --- /dev/null
  +++ b/planetary-explorer/web-ui/src/utils/mapHistory.ts
  @@ -0,0 +1,101 @@
  +import { ChatHistoryContext, MapContext } from '../services/api';
  +import type { TileJsonFetchOptions, TileJsonResult } from './tileJsonFetcher';
  +
  +const RESTORABLE_MODULES = new Set([
  +  'building_damage',
  +  'extreme_weather',
  +  'forecast',
  +  'foundation_change',
  +  'resilience',
  +  'site_audit',
  +  'terrain',
  +  'timeseries',
  +  'vision',
  +]);
  +
  +export function restorableHistoryModule(module?: string): string | null {
  +  return module && RESTORABLE_MODULES.has(module) ? module : null;
  +}
  +
  +export function restoredLayerFromContext(map?: Partial<MapContext>): any | null {
  +  if (!map) return null;
  +  const savedTiles = map.tile_urls?.filter((tile) => Boolean(tile.tilejson_url)) || [];
  +  const fallbackTile = savedTiles[0];
  +  const tileUrl = map.imagery_url || fallbackTile?.tilejson_url;
  +  if (!tileUrl) return null;
  +  const bounds = map.bounds
  +    ? [map.bounds.west, map.bounds.south, map.bounds.east, map.bounds.north]
  +    : undefined;
  +  const collection = map.current_collection || fallbackTile?.collection || 'restored-layer';
  +  const itemId = map.item_id || fallbackTile?.item_id || 'restored-item';
  +  const tileBounds = bounds || [-180, -85, 180, 85];
  +  const allTileUrls = savedTiles.map((tile, index) => ({
  +      item_id: tile.item_id || `${itemId}-${index + 1}`,
  +      bbox: tile.bbox || tileBounds,
  +      tilejson_url: tile.tilejson_url,
  +    }));
  +  return {
  +    bbox: bounds,
  +    items: [
  +      {
  +        id: itemId,
  +        collection,
  +        datetime: map.datetime || '',
  +        tile_url: tileUrl,
  +      },
  +    ],
  +    tile_url: tileUrl,
  +    preview_url: tileUrl,
  +    all_tile_urls: allTileUrls.length > 0 ? allTileUrls : undefined,
  +  };
  +}
  +
  +export interface ChatHistoryMapRestore {
  +  token: number;
  +  context: ChatHistoryContext;
  +}
  +
  +interface LeafletTileData {
  +  bbox?: number[];
  +  tile_url?: string;
  +  all_tile_urls?: Array<{
  +    tilejson_url: string;
  +    bbox?: number[];
  +  }>;
  +}
  +
  +export async function resolveLeafletTileSources(
  +  data: LeafletTileData,
  +  collection: string,
  +  fetchTileJson: (
  +    url: string,
  +    options: TileJsonFetchOptions,
  +  ) => Promise<TileJsonResult>,
  +): Promise<Array<{ tileTemplate: string; bbox?: number[] }>> {
  +  const sources = data.all_tile_urls?.length
  +    ? data.all_tile_urls.map((tile) => ({
  +        url: tile.tilejson_url,
  +        bbox: tile.bbox,
  +      }))
  +    : data.tile_url
  +      ? [{ url: data.tile_url, bbox: data.bbox }]
  +      : [];
  +
  +  const resolved = await Promise.all(sources.map(async (source) => {
  +    if (
  +      source.url.includes('{z}') &&
  +      source.url.includes('{x}') &&
  +      source.url.includes('{y}')
  +    ) {
  +      return { tileTemplate: source.url, bbox: source.bbox };
  +    }
  +    const result = await fetchTileJson(source.url, { collection });
  +    return result.success && result.tileTemplate
  +      ? { tileTemplate: result.tileTemplate, bbox: source.bbox || result.tilejson?.bounds }
  +      : null;
  +  }));
  +
  +  return resolved.filter(
  +    (source): source is { tileTemplate: string; bbox?: number[] } => source !== null,
  +  );
  +}
  \ No newline at end of file
  </full_diff>
</commit_history>
